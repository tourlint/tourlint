/**
 * `/docs` 맨 위 소개글. 심사위원이 이 주소를 직접 연다 — 무엇을 하는 API 인지, 어떤 순서로
 * 읽으면 되는지, 여기서 어떻게 불러 보는지를 먼저 말한다.
 */
export const INTRO = `
**TourLint** 는 여행사가 만든 관광상품 일정을 **한국관광공사 관광정보로 출시 전에 검수**하는 서비스입니다.
쉬는 날에 방문하는 곳, 끝난 행사, 겹치는 시간, 모자란 이동시간, 빠진 식사, 타깃에 맞지 않는 구성을 찾아내고
고칠 방법(수정안)을 함께 제시합니다. 이 페이지는 TourLint API 를 **서비스를 쓰는 순서대로** 정리한 것입니다.

- 서비스: [tourlint-web.up.railway.app](https://tourlint-web.up.railway.app/)
- 서버 상태: [\`GET /health\`](/health)

### 읽는 순서

| 순서 | 묶음 | 하는 일 |
|---|---|---|
| 0 | 서비스 상태 | 로그인 없이 서버 상태와 배포된 커밋을 본다 |
| 1 | 인증 | 로그인하면 세션 쿠키가 발급된다 |
| 2 · 3 | 상품 · 일정 입력 | 상품을 만들고 일정을 넣는다 — 엑셀 · CSV, 자연어, 직접 입력 |
| 4 · 5 | 장소 연결 · 기획 | 일정의 이름을 관광정보의 실제 장소에 연결하고, 근처 장소를 담는다 |
| 6 | 검수 | 규칙 R01 ~ R10 으로 판정하고 출시 준비도 점수를 낸다 |
| 7 | 수정안 | 고칠 방법을 골라 미리 보고, 확정하면 자동으로 다시 검수한다 |
| 8 | 리포트 · 출시 | PDF 리포트를 만들고 출시를 승인한다 |
| 9 | 레이더 | 검수한 상품의 관광정보가 바뀌었는지, 관심 지역에 새 소식이 있는지 본다 |
| 10 | 검수 기준 | 표준과 회사 기준, 규칙 설명 |
| 11 | 기준 코드 · 호출량 | 지역 · 분류 코드, 외부 API 호출 예산과 호출 기록 |

### 이 페이지에서 직접 불러 보기

1. 아래 **인증** 묶음의 \`POST /api/v1/auth/login\` 을 펼쳐 **Try it out** 을 누르고, 제출 서류의 테스트 계정으로 본문을 채운 뒤 **Execute**.
2. \`200\` 이 오면 브라우저에 세션 쿠키(\`tourlint_session\`)가 저장됩니다. 이 페이지와 API 가 같은 주소라 **이후 호출에 쿠키가 자동으로 붙습니다.**
   자물쇠 표시는 로그인이 필요하다는 뜻이고, 오른쪽 위 **Authorize** 는 누르지 않아도 됩니다.
3. 조회부터 차례로 불러 보세요 — \`GET /api/v1/products\` → \`GET /api/v1/products/{productId}\` → \`GET /api/v1/products/{productId}/audit-runs\` → \`GET /api/v1/audit-runs/{runId}/findings\`.

> 테스트 계정은 여러 심사위원이 함께 씁니다. 저장 · 수정 · 삭제 API(POST · PUT · PATCH · DELETE)는 **직접 만든 상품에만** 불러 주세요.
> 그래서 일정 · 결과를 바꾸는 API 는 상품 · 항목 번호를 미리 채워 두지 않았습니다. 조회 API 에는 예시 상품(38)의 번호가 채워져 있습니다.
> 데모 데이터 복원(\`POST /api/v1/demo/reset\`)은 이 계정의 상품을 모두 지우고 처음 상태로 되돌리므로 이 페이지에 싣지 않았습니다.
> 로그인하지 않은 상태로 부르면 \`401 NOT_AUTHENTICATED\` 가 옵니다.

### 형식

- 요청 · 응답은 JSON 입니다. 시각은 ISO 8601(UTC), 날짜는 \`YYYY-MM-DD\`, 일정 시각은 \`HH:mm\`(한국 시간)입니다.
- 목록은 대부분 \`content\` · \`page\` · \`size\` · \`totalElements\` 모양이고 \`page\` 는 0부터입니다. 장소 담기 목록(\`GET /api/v1/plan/places\`)만 1쪽부터 20곳씩입니다.
- 검수는 오래 걸려서 **비동기**입니다 — 요청하면 \`202\` 와 작업 번호를 받고, 작업이 끝나면 결과를 읽습니다.
- 오류는 모두 같은 모양입니다. \`reasonCode\` 는 43종 중 하나이고, \`message\` 는 무엇이 · 왜 · 다음에 무엇을 할지 담은 한국어 문장입니다. 형식이 틀린 값이나 깨진 JSON 을 보내면 \`400 INPUT_INVALID\` 입니다.

\`\`\`json
{
  "reasonCode": "NOT_FOUND",
  "message": "상품을 찾을 수 없습니다. 목록에서 다시 선택해 주세요.",
  "unit": "PRODUCT",
  "traceId": "9f2c1a7b3e4d5f60",
  "occurredAt": "2026-09-20T01:23:45.678Z"
}
\`\`\`

- 다른 계정의 상품 · 검수 결과는 \`403\` 이 아니라 \`404\` 입니다 — 있는지 없는지도 알리지 않습니다.

### 외부 데이터

| 제공자 | 쓰는 곳 | 표시 |
|---|---|---|
| 한국관광공사 국문 관광정보 · 무장애 · 반려동물 · 연관 관광지 · 두루누비 · 지역별 방문자수 | 장소 검색 · 운영정보 · 행사 · 걷기 길 · 레이더 | 공사 원문 |
| 카카오모빌리티 | 구간별 자동차 이동시간 (R08) | 외부 참고 |
| 기상청 | 단기 · 중기 예보, 평년값 (R09) | 외부 참고 |
| 생성형 AI | 자연어 일정 정형화, 장소 후보 제안, 전화로 물어볼 내용, 오늘 할 일, 운영시간 문구 해석 보조 | AI 정규화 |

- 판정 근거가 되는 **공사 원문은 서버에 저장하지 않습니다.** 필요할 때 조회해 응답으로만 흘려보냅니다.
- **AI 는 정리까지만 합니다.** 판정(차단 · 오류 · 주의)은 규칙 R01 ~ R10 이 내고, AI 가 내놓은 후보는 사람이 골라야 일정에 들어갑니다.
- 공사 관광정보는 하루 호출 예산을 나눠 씁니다. 예산을 다 쓰면 검수 · 장소 담기 조회와 관광정보를 쓰는 AI 도우미(장소 찾기 · 물어볼 내용)가 \`429 BUDGET_EXHAUSTED\` 로 거절되고, 일정 입력과 저장은 계속됩니다(\`GET /api/v1/usage/budget\`).
- 공사 데이터는 당일 변경분이 다음 날 반영되므로 출발이 임박하면 운영기관에 최종 확인을 권장합니다.

출처: ⓒ한국관광공사
`.trim();
