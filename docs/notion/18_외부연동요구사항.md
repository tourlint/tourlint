<callout icon="🔌" color="gray_bg">
	**TourLint 외부 연동 요구사항**
	TourLint가 의존하는 외부 서비스는 **4계열 14개 연동점**뿐이다 — 한국관광공사 OpenAPI 9종, 카카오모빌리티 길찾기 2종, 기상청 데이터 2종 + 평년값 테이블, LLM 1종.
	이 문서의 모든 요청 · 응답 계약은 **실호출로 검증한 사실**에 근거한다(공사 · 카카오 2026.08.12–13, 기상청 2026.08.26). 추정으로 적은 항목은 없다.
</callout>
<table fit-page-width="true" header-column="true">
<tr>
<td>문서</td>
<td>통합 요구사항 명세서 – 외부 연동 요구사항</td>
</tr>
<tr>
<td>버전</td>
<td>v1.7</td>
</tr>
<tr>
<td>기준일</td>
<td>2026.08.28</td>
</tr>
<tr>
<td>선행 문서</td>
<td>용어 정의 · 서비스 범위 · 기능 · 권한 요구사항</td>
</tr>
<tr>
<td>후속 문서</td>
<td>데이터 요구사항(스키마), 비기능 요구사항(성능 · 가용성), 예외처리 요구사항(장애 대응)</td>
</tr>
<tr>
<td>검증 근거</td>
<td>KorService2 실호출 1차 12콜 · 2차 82콜 (2026.08.12 – 13), 카카오모빌리티 2콜 (2026.08.13)</td>
</tr>
</table>
<table_of_contents/>
---
## 1. 문서 개요
### 1-1. 목적
이 문서는 외부 서비스별로 **무엇을, 어떤 파라미터로 호출하고, 응답의 어떤 필드를 어디에 쓰는지**를 계약 수준으로 고정한다. 여기 없는 외부 호출은 구현하지 않는다 (SC-DT-010 · SC-DT-011).
### 1-2. ID 체계
<table fit-page-width="true" header-row="true">
<tr>
<td>접두어</td>
<td>대상</td>
</tr>
<tr>
<td>`EI-CM`</td>
<td>전 연동 공통 정책</td>
</tr>
<tr>
<td>`EI-KT`</td>
<td>한국관광공사 OpenAPI (`KorService2`)</td>
</tr>
<tr>
<td>`EI-KM`</td>
<td>카카오모빌리티 길찾기 API</td>
</tr>
<tr>
<td>`EI-WX`</td>
<td>기상청 예보 · 평년값</td>
</tr>
<tr>
<td>`EI-LM`</td>
<td>LLM API</td>
</tr>
</table>
### 1-3. 연동 지도
<table fit-page-width="true" header-row="true">
<tr>
<td>계열</td>
<td>연동점</td>
<td>사용 기능</td>
<td>실호출 검증</td>
</tr>
<tr>
<td>공사 OpenAPI</td>
<td>9개 오퍼레이션</td>
<td>F02 – F14 전반, R01 – R06 · R10, T1 · T2</td>
<td>✅ 2026.08.05 – 08.13</td>
</tr>
<tr>
<td>카카오모빌리티</td>
<td>자동차 길찾기 · 미래 운행 정보 길찾기</td>
<td>R08, F10 이동 지표</td>
<td>✅ 2026.08.13</td>
</tr>
<tr>
<td>기상청</td>
<td>단기예보 · 중기예보 조회서비스, 평년 강수일수 테이블</td>
<td>R09</td>
<td>공공데이터포털 자동승인 (신청 시 즉시)</td>
</tr>
<tr>
<td>LLM</td>
<td>구조화 · 정규화 · 설명 생성</td>
<td>F01 · F03, finding 메시지</td>
<td>–</td>
</tr>
</table>
---
## 2. 공통 연동 정책 (EI-CM)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EI-CM-001</td>
<td>모든 외부 API 호출은 **서버에서만** 수행해야 한다. 브라우저에서 외부 API를 직접 호출하지 않는다. (PM-SC-002)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-002</td>
<td>모든 인증키는 환경변수로 주입해야 하며, 소스코드 · 리포지토리 · 로그 · 오류 메시지에 노출하지 않아야 한다. (PM-SC-001 · 003)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-003</td>
<td>외부 호출은 연동점별 **어댑터 계층**을 경유해야 한다. 규칙엔진 · 화면 코드가 외부 API를 직접 호출하지 않는다. 어댑터는 요청 생성 · 응답 파싱 · 재시도 · 로깅을 담당한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-004</td>
<td>모든 외부 호출에 타임아웃을 설정해야 한다 — 연결 3초, 응답 10초. 타임아웃 값은 환경변수로 조정 가능해야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-005</td>
<td>실패한 호출은 지수 백오프로 **최대 2회 재시도**하고, 그래도 실패하면 콘텐츠 단위 확인 불가로 확정해야 한다. (FR-AU-027 · 028)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-006</td>
<td>모든 외부 호출은 호출 로그(제공자 · 오퍼레이션 · 시각 · 상태 · 응답시간)에 기록되어야 한다. 공사 호출은 예산 카운트에 합산한다. (FR-OP-001)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-007</td>
<td>외부 API 장애 시 화면 전체가 실패하지 않아야 하며, 해당 데이터 영역만 대체 문구로 처리해야 한다. (UI-ST-004)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-008</td>
<td>외부 데이터로 산출한 판정에는 `외부 참고` 출처 배지와 제공자명을 표기해야 한다. (FR-AU-033 · UI-CM-011)</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-009</td>
<td>외부 응답의 원문 저장 여부는 데이터 요구사항의 저장 경계를 따른다. 공사 원문은 저장하지 않고, 카카오모빌리티 · 기상청 산출값(소요시간 · 거리 · 강수확률)은 자체 산출물의 입력값으로서 finding에 기록할 수 있다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-CM-010</td>
<td>본 문서에 정의되지 않은 외부 연동을 추가할 때는 서비스 범위 요구사항(SC-CM-001)의 개정 절차를 거쳐야 한다.</td>
<td>M</td>
</tr>
</table>
---
## 3. 한국관광공사 OpenAPI (EI-KT)
### 3-1. 연결 정보
<table fit-page-width="true" header-column="true">
<tr>
<td>베이스 URL</td>
<td>`https://apis.data.go.kr/B551011/KorService2`</td>
</tr>
<tr>
<td>인증</td>
<td>쿼리 파라미터 `serviceKey` — 공공데이터포털 일반 인증키(**Decoding**)</td>
</tr>
<tr>
<td>공통 파라미터</td>
<td>`MobileOS=ETC` · `MobileApp=TourLint` · `_type=json`</td>
</tr>
<tr>
<td>호출 한도</td>
<td>개발계정 1일 1,000건 / 운영계정 1일 100,000건</td>
</tr>
<tr>
<td>데이터 신선도</td>
<td>국문 콘텐츠는 04:30 이후 최신 반영. 당일 수정분은 익일 조회 (D+1)</td>
</tr>
<tr>
<td>키 관리</td>
<td>팀 대표 1인 활용신청 · 유효기간 2년 · 인코딩키와 디코딩키는 1차 심사 제출 항목</td>
</tr>
</table>
### 3-2. 공통 요구사항
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EI-KT-001</td>
<td>공사 OpenAPI는 아래 **9개 오퍼레이션만** 호출해야 한다. 폐기 예정 3종(지역코드 조회 · 서비스코드 조회 · 생태관광정보 서비스)과 반복정보 조회(`detailInfo2`)는 호출하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-002</td>
<td>응답은 `_type=json`으로 수신하되, **본문이 ****`&lt;`****로 시작하는 XML 오류 응답**(인증 실패 · 쿼터 초과)을 별도 처리해야 한다. JSON 파싱 실패를 서버 오류로 오인하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-003</td>
<td>`resultCode`가 `0000`이 아닌 응답은 실패로 처리하고 코드별 대응(쿼터 초과 → 예산 관리자 통지, 키 오류 → 관리자 알림)을 수행해야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-004</td>
<td>`items`가 빈 문자열 `""`로 오는 0건 응답을 정상 처리해야 한다. (실측: 0건 시 `items`는 배열이 아니라 빈 문자열)</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-005</td>
<td>`items.item`이 1건일 때 객체, 2건 이상일 때 배열로 오는 양쪽 형태를 모두 처리해야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-006</td>
<td>목록 조회 응답에 포함된 구 코드체계 필드(`areacode` · `sigungucode` · `cat1/2/3`)는 값이 있어도 **읽지 않아야 한다.** 지역은 `lDongRegnCd` · `lDongSignguCd`, 유형은 `contentTypeId` · `lclsSystm1/2/3`만 사용한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-007</td>
<td>상세 조회(공통정보 · 소개정보) 관광지 단위 호출은 **병렬**로 수행해야 한다. (FR-AU-025)</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-008</td>
<td>위치기반 목록 조회의 반경은 20,000m(20km)를 초과하지 않아야 한다. (SC-DT-013)</td>
<td>M</td>
</tr>
</table>
### 3-3. 오퍼레이션별 계약
<callout icon="📋" color="blue_bg">
	각 오퍼레이션의 파라미터와 사용 필드는 2026.08.05 – 08.13 실호출 응답에서 확인한 것만 기재한다. 상세 스키마(전체 필드 목록)는 `probe_out/` · `probe2_out/` 원본 JSON을 규범 참조로 삼는다.
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>#</td>
<td>오퍼레이션</td>
<td>핵심 파라미터</td>
<td>사용 필드</td>
<td>소비 기능</td>
</tr>
<tr>
<td>1</td>
<td>`searchKeyword2` 키워드 검색</td>
<td>`keyword` · `numOfRows` · `pageNo` · `arrange=A`</td>
<td>`contentid` · `title` · `addr1` · `contenttypeid` · `lDongRegnCd` · `lDongSignguCd`</td>
<td>F02 관광지 매칭 (전 규칙 선행)</td>
</tr>
<tr>
<td>2</td>
<td>`detailCommon2` 공통정보</td>
<td>`contentId`</td>
<td>`mapx` · `mapy` · `modifiedtime` · `createdtime` · `homepage` · `firstimage` · `cpyrhtDivCd` · `lclsSystm1/2/3` · `overview`(표시 전용)</td>
<td>R06 · R08 · F02 확정 정보</td>
</tr>
<tr>
<td>3</td>
<td>`detailIntro2` 소개정보</td>
<td>`contentId` · `contentTypeId`</td>
<td>`contentTypeId`별 분기 — 휴무일 · 운영시간 · 문의처 (아래 분기표)</td>
<td>R01 · R05 · F03 · F07</td>
</tr>
<tr>
<td>4</td>
<td>`searchFestival2` 행사정보</td>
<td>`eventStartDate` · `eventEndDate` · `lDongRegnCd` · `lDongSignguCd`</td>
<td>`eventstartdate` · `eventenddate` · `title` · `progresstype`</td>
<td>R02 · T2</td>
</tr>
<tr>
<td>5</td>
<td>`areaBasedSyncList2` 동기화 목록</td>
<td>`modifiedtime`(해당일) · `showflag` · `numOfRows`(1000)</td>
<td>`contentid` · `modifiedtime` · `showflag` · `createdtime` · `cpyrhtDivCd`</td>
<td>F12 배치 1단계 · R06 · T1</td>
</tr>
<tr>
<td>6</td>
<td>`areaBasedList2` 지역기반 목록</td>
<td>`lDongRegnCd` · `lDongSignguCd` · `lclsSystm1/2/3`(선택 필터) · `arrange=D`(생성일 내림차순)</td>
<td>`contentid` · `contenttypeid` · `createdtime` · `lclsSystm1/2/3` · `mapx` · `mapy`</td>
<td>수정안 후보 보강 · F13 · F14 · **T1**</td>
</tr>
<tr>
<td>7</td>
<td>`locationBasedList2` 위치기반 목록</td>
<td>`mapX` · `mapY` · `radius`(≤20000) · `arrange=S`(거리순)</td>
<td>`contentid` · `dist`(m) · `contenttypeid` · `lclsSystm1/2/3`</td>
<td>R01 · R02 · R08 대체 관광지</td>
</tr>
<tr>
<td>8</td>
<td>`ldongCode2` 법정동 코드</td>
<td>(없음 = 시도 목록) · `lDongRegnCd`(= 시군구 목록)</td>
<td>`code` · `name`</td>
<td>F01 지역 선택 2단 UI · F13 조건 2</td>
</tr>
<tr>
<td>9</td>
<td>`lclsSystmCode2` 분류체계 코드</td>
<td>(없음 = 대분류) · `lclsSystm1` · `lclsSystm2` 계층 조회</td>
<td>`code` · `name` — 대 10 · 중 59 · 소 246</td>
<td>R04 · R09 · R10 기준표 · 설정 초기값</td>
</tr>
</table>
**소개정보 조회의 ****`contentTypeId`****별 필드 분기 (실측 확정)**
<table fit-page-width="true" header-row="true">
<tr>
<td>contentTypeId</td>
<td>휴무일</td>
<td>운영시간</td>
<td>문의처</td>
</tr>
<tr>
<td>12 관광지</td>
<td>`restdate`</td>
<td>`usetime`</td>
<td>`infocenter`</td>
</tr>
<tr>
<td>14 문화시설</td>
<td>`restdateculture`</td>
<td>`usetimeculture`</td>
<td>`infocenterculture`</td>
</tr>
<tr>
<td>15 축제공연행사</td>
<td>–</td>
<td>`playtime`</td>
<td>`sponsor1tel`</td>
</tr>
<tr>
<td>28 레포츠</td>
<td>`restdateleports`</td>
<td>`usetimeleports` — ⚠️ 실측상 `  • 입실 15:00- 퇴실 11:00` 처럼 **체크인/아웃 시각**이 담기는 경우가 있다. 운영시간으로 읽으면 심야 영업으로 오판한다 (docs/[파싱규칙.md](http://파싱규칙.md) §3)</td>
<td>`infocenterleports`</td>
</tr>
<tr>
<td>32 숙박</td>
<td>– (R01 제외)</td>
<td>`checkintime` / `checkouttime`</td>
<td>`infocenterlodging`</td>
</tr>
<tr>
<td>38 쇼핑</td>
<td>`restdateshopping`</td>
<td>`opentime`</td>
<td>`infocentershopping`</td>
</tr>
<tr>
<td>39 음식점</td>
<td>`restdatefood`</td>
<td>`opentimefood`</td>
<td>`infocenterfood`</td>
</tr>
</table>
### 3-4. 실측으로 확정된 동작 (구현 시 전제)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>확정 동작</td>
<td>등급</td>
</tr>
<tr>
<td>EI-KT-010</td>
<td>`searchFestival2`의 `eventStartDate`는 "그 날짜 이후 시작"이 아니라 **"그 날짜에 아직 종료되지 않은 행사"** 를 반환한다. (실측: 10.22 기준 조회 시 10.21 시작 행사가 반환됨) 여행기간 겹침 조회는 `eventStartDate=여행시작일`로 충분하며 추가 보정 로직을 넣지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-011</td>
<td>`areaBasedSyncList2`의 `modifiedtime`은 **해당일 분만** 반환하고 누적되지 않는다. 배치는 직전 성공일 다음 날부터 어제까지 날짜를 순회 호출한다. (FR-MO-011) **건수는 조회 시각에 따라 크게 변동한다** — 동일 일자 기준으로 새벽 02시 11건, 오후 3시 177건이 반환됐다(2026.08.20 실측). 특정 건수를 상수로 가정한 로직을 두지 않고, 동기화 지연 판단은 FR-MO-015의 **0건 조건**으로만 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-012</td>
<td>`areaBasedSyncList2`에서 `showflag` 미지정 시 표출 · 비표출이 모두 반환된다. 비표출 감지를 위한 별도 호출을 하지 않는다. (FR-MO-012)</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-013</td>
<td>`numOfRows`는 2000까지 동작 확인됐으나 안전 마진으로 **1000**을 사용한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-014</td>
<td>`ldongCode2` 시도 코드는 길이를 가정하지 않는다. (세종특별자치시 = `36110`, 5자리) 지역 목록은 항상 API 응답을 사용하고 하드코딩하지 않는다. (FR-IN-006) **목록 구성은 실제로 변한다** — 2026.08.20 재호출에서 `12 전남광주통합특별시`가 확인됐다. 시도명·코드를 상수로 박은 테스트나 드롭다운을 두지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-015</td>
<td>`lclsSystmCode2`는 파라미터 없이 호출하면 **대분류 10개만** 반환한다. 중 · 소분류는 `lclsSystm1` · `lclsSystm2`를 지정한 계층 호출로 수집하며, 전체 수집(약 70콜)은 배포 시 1회 수행해 설정 초기값으로 저장한다. 분류체계는 자체 산출 기준표의 입력이므로 저장 금지 대상이 아니다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-016</td>
<td>운영시간 · 휴무일 원문에는 서식 접두 기호(`※`, `  • `, `-`)와 대괄호 요일 라벨이 포함될 수 있다. 해석기는 FR-AU-014 – 016의 전처리를 전제로 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-017</td>
<td>`cpyrhtDivCd`는 실측상 대부분 Type3(변경금지)다. 값이 없을 때만 표기를 생략하고, Type1 · Type3 각각의 배지 규칙(FR-CM-011)을 적용한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-018</td>
<td>**상세 조회(****`detailIntro2`****)의 구 코드체계 필드는 값이 아예 비어 온다** — `areacode` · `sigungucode` · `cat1/2/3` 전부 빈 문자열(2026.08.20 실측). EI-KT-006이 "읽지 않는다"고 규정한 것은 정책이지만, 상세 조회에서는 **사실상 선택지가 없다.** 지역은 `lDongRegnCd` · `lDongSignguCd`, 유형은 `lclsSystm1/2/3`를 목록 조회에서 수집해 쓴다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-019</td>
<td>문의처 필드에는 **기관명과 전화번호가 한 문자열로 섞여** 온다(예: `㈜강릉농산물도매시장 관리소 033-646-4654`, `경포여행자센터 033-640-4531`). 번호 범위 표기(`033-660-3301~8`)도 존재한다. 전화번호만 사용할 때는 추출 전처리를 거치고, 원문은 그대로 보존한다 (docs/[파싱규칙.md](http://파싱규칙.md) §4).</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-020</td>
<td>문화시설(14)의 `spendtime`은 실측상 **빈 문자열**이다. 소요시간을 공사 데이터에서 취득한다고 전제하지 않고, 중분류별 기본 체류시간 테이블(FR-IN-011)만 사용한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KT-021</td>
<td>`areaBasedList2`의 `arrange`는 **`D` = 생성일 내림차순 · `C` = 수정일 내림차순**이며, 지정하지 않으면 정렬되지 않는다(2026.08.28 실측, 강원 강릉 51/150 각 10건). T1(최근 30일 신규 등록)은 `arrange=D`로 첫 페이지부터 읽어 `createdtime`이 기준일보다 이르면 멈춘다 — 강릉 첫 10건 중 최근 30일은 2건이었다. 정렬 없이 훑으면 한 지역 `totalCount`가 1,005건이라 상품당 열 콜이 넘는다. (FR-RU-110)</td>
<td>M</td>
</tr>
</table>
### 3-5. 호출량 계약
<table fit-page-width="true" header-row="true">
<tr>
<td>시나리오</td>
<td>호출량</td>
<td>근거</td>
</tr>
<tr>
<td>1박 2일 상품 1건 검수</td>
<td>약 29콜 (키워드 8 + 공통 8 + 소개 8 + 위치기반 3 + 행사 2)</td>
<td>FR-OP 호출량표</td>
</tr>
<tr>
<td>2박 3일 상품 1건 검수 (최대 12곳)</td>
<td>약 43콜 (키워드 12 + 공통 12 + 소개 12 + 위치기반 4 + 행사 3)</td>
<td>FR-OP 호출량표</td>
</tr>
<tr>
<td>경량 배치 1회 (평일 05:00)</td>
<td>약 4 – 6콜 (동기화 목록 1 – 3 + 관련 상세 약 3)</td>
<td>FR-MO-011 – 013</td>
</tr>
<tr>
<td>비표출 대량 전환일 (예: 2026-07-28 3,206건)</td>
<td>페이지 상한 20 이내, 초과 시 중단 후 자체 `contentid` 개별 확인</td>
<td>FR-MO-016</td>
</tr>
<tr>
<td>분류체계 전체 수집 (배포 시 1회)</td>
<td>약 70콜</td>
<td>EI-KT-015</td>
</tr>
</table>
---
## 4. 카카오모빌리티 길찾기 API (EI-KM)
### 4-1. 연결 정보
<table fit-page-width="true" header-column="true">
<tr>
<td>베이스 URL</td>
<td>`https://apis-navi.kakaomobility.com`</td>
</tr>
<tr>
<td>인증</td>
<td>요청 헤더 `Authorization: KakaoAK {REST_API_KEY}` — 카카오디벨로퍼스 앱(TourLint)의 REST API 키</td>
</tr>
<tr>
<td>무료 쿼터</td>
<td>자동차 길찾기 10,000건/일 · 미래 운행 정보 길찾기 5,000건/일 (초과 시 건당 8원)</td>
</tr>
<tr>
<td>좌표계</td>
<td>WGS84 `경도,위도` — 공사 `mapx`,`mapy`를 **변환 없이 그대로** 사용 (2026.08.13 실호출 확인)</td>
</tr>
<tr>
<td>선정 사유</td>
<td>후보 중 유일하게 미래 출발 시각 기준 경로 제공. 네이버클라우드는 무료 종료(2025.07), TMAP은 1,000건/일 + 미래 시각 미지원으로 백업 후보</td>
</tr>
</table>
### 4-2. 요구사항
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EI-KM-001</td>
<td>R08 이동시간 산출의 **1급 경로는 미래 운행 정보 길찾기**(`GET /v1/future/directions`)다. `departure_time`에 상품의 일정 항목 출발 시각을 `YYYYMMDDHHMM` 형식으로 전달한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-002</td>
<td>출발 시각이 과거이거나 미래 운행 정보 호출이 실패하면 자동차 길찾기(`GET /v1/directions`)로 폴백하고, 화면에 "현재 시각 기준" 표기를 부착해야 한다. (FR-RU-085)</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-003</td>
<td>요청은 `origin={경도},{위도}` · `destination={경도},{위도}` · `summary=true`로 구성해야 한다. 상세 경로(`roads` · `guides`)는 사용하지 않으므로 요청하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-004</td>
<td>응답에서 사용하는 필드는 `routes[0].result_code` · `summary.distance`(m) · `summary.duration`(초)로 한정한다. `fare` · `taxi` 요금 정보는 사용하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-005</td>
<td>`result_code`가 0이 아니면(경로 없음 · 좌표 오류 등) 해당 구간을 확인 불가로 처리하고 사유를 기록해야 한다. HTTP 200이어도 `result_code`를 반드시 확인한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-006</td>
<td>호출 단위는 **연속한 두 일정 항목의 구간**이다. 관광지 8곳 상품 기준 구간 7개 = 7콜, 12곳(2박 3일) 기준 구간 11개 = 11콜. 동일 구간 · 동일 출발 시각의 재호출은 검수 실행 내에서 캐시해 1콜로 처리한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-007</td>
<td>이동수단이 전세버스 · 자가용이면 자동차 경로를 사용한다. **대중교통이면 호출하지 않고** R08을 확인 불가로 처리한다. (FR-RU-086)</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-008</td>
<td>카카오모빌리티 산출 판정에는 `외부 참고` 배지와 제공자명을 표기해야 한다. 공사 판정과 시각적으로 분리한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-KM-009</td>
<td>API 전면 장애 시 R08 전체를 확인 불가로 처리하고 검수를 계속 진행해야 한다. 직선거리 기반 추정으로 대체하지 않는다.</td>
<td>M</td>
</tr>
</table>
**실호출 검증 기록 (2026.08.13)**
```plain text
구간: 강릉 중앙시장(128.898632,37.753996) → 오죽헌(128.877841,37.779214)

현재 시각 기준          /v1/directions          distance 5,557m · duration 886초
2026-10-28 10:00 기준   /v1/future/directions   distance 4,762m · duration 785초

→ 동일 구간이 출발 시각에 따라 다른 경로 · 소요시간으로 산출됨을 확인.
  미래 운행 정보를 1급 경로로 두는 EI-KM-001의 실증 근거.
```
---
## 5. 기상청 데이터 (EI-WX)
### 5-1. 3단 판정 근거와 연동점
<table fit-page-width="true" header-row="true">
<tr>
<td>여행 일자까지 (해당 일정이 속한 날짜 기준 — FR-RU-091)</td>
<td>연동점</td>
<td>방식</td>
<td>화면 표기</td>
</tr>
<tr>
<td>D+0 – D+2</td>
<td>기상청_단기예보 조회서비스 (공공데이터포털)</td>
<td>실시간 호출</td>
<td>"단기예보 기준 — 강수확률 N%"</td>
</tr>
<tr>
<td>D+3</td>
<td>기상청_단기예보 조회서비스 (부분 커버리지)</td>
<td>실시간 호출. 예보가 그 날 **야외 일정 시간대를 덮을 때만** 판정하고, 못 덮으면 확인 불가. 2026.08.26 실측에서는 조회 가능한 발표분 전부(어제 23시 ~ 오늘 14시)가 D+3 21시까지 덮었다</td>
<td>"단기예보 기준 — 강수확률 N%"</td>
</tr>
<tr>
<td>D+4 – D+10</td>
<td>기상청_중기예보 조회서비스 `MidFcstInfoService` (공공데이터포털)</td>
<td>실시간 호출</td>
<td>"중기예보 기준 — 강수확률 N%"</td>
</tr>
<tr>
<td>D+11 이상</td>
<td>기상자료개방포털 평년 강수일수 통계</td>
<td>**사전 구축 고정 테이블** (지역 × 월)</td>
<td>"평년 기준 — N월 강수일수 N.N일 (N%)"</td>
</tr>
</table>
<callout icon="✅" color="green_bg">
	평년 강수일수를 파일로 받아 고정 테이블로 쓰는 것은 **축소안이 아니라 정상 구현**이다. 실시간 호출 의무는 한국관광공사 데이터에만 적용되며(SC-DT-014), 기상자료개방포털 통계는 공공누리 제1유형(출처표시)이다.
</callout>
### 5-2. 요구사항
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EI-WX-001</td>
<td>단기 · 중기예보는 공공데이터포털 활용신청(자동승인)을 거친 인증키로 호출해야 한다. **키 문자열 자체는 공사 인증키와 같다** — data.go.kr 은 계정당 일반 인증키 하나를 발급하고 승인된 서비스 전부에 그 키를 쓴다. 인증(계정 키)과 인가(서비스별 활용신청)가 분리돼 있어 **활용신청은 서비스마다 따로** 해야 하며, 승인 없이 호출하면 키 오류가 아니라 접근 거부로 떨어진다. 환경변수는 `KMA_SERVICE_KEY` 로 분리해 둔다 — 어댑터가 다른 연동의 설정을 읽으면 안 된다(NF-MT-003).</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-002</td>
<td>단기예보에서는 강수확률(POP) 항목만 사용한다. 격자 좌표 변환은 상품 여행 지역의 대표 지점 1개로 수행하며, 일정 항목별 개별 조회를 하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-003</td>
<td>중기예보에서는 중기육상예보의 오전 · 오후 강수확률을 사용하고, 두 값 중 큰 값을 해당일 값으로 취한다. 응답 범위는 **발표 시각에 따라 다르다** — 06시 발표는 `rnSt4Am` ~ `rnSt10`(발표일 +4 ~ +10), 18시 발표는 `rnSt5Am` ~ `rnSt10`(+5 ~ +10)이다. 필드 번호는 발표일로부터의 일수이며 +8일부터는 오전 · 오후 구분 없이 한 값이다. 발표 시각(06 · 18시) 중 **판정 대상 일자를 덮는 발표분**을 고른다 — 최신 발표분을 쓰면 가까운 날이 창 밖으로 밀린다. **18시 발표는 그날의 D+4 를 잃으므로 저녁에도 06시 발표분을 써야 D+4 가 닿는다.** 과거 발표분은 **오늘 기준 1일 전까지만** 조회되고 실제로 남는 것은 최근 2개 발표분뿐이다(2026.08.26 기준 08-25 06시는 `NO_DATA`).</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-004</td>
<td>평년 강수일수 테이블은 기상자료개방포털에서 시도 대표 지점 × 12개월로 사전 구축하고, 출처(`출처: 기상청 기상자료개방포털`)와 기준 평년(예: 1991 – 2020)을 함께 저장 · 표기해야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-005</td>
<td>강수 지표의 임계치는 R09 판정 규칙을 따르고, 판정 근거 종류(단기 · 중기 · 평년)를 finding에 기록해야 한다. (FR-RU-092)</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-006</td>
<td>예보 API 호출이 실패하면 해당 구간을 평년 테이블 판정으로 **자동 강등**하고 "평년 기준" 표기를 부착해야 한다. R09 전체를 확인 불가로 만들지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-WX-007</td>
<td>예보 조회 결과는 발표분 단위로 캐시할 수 있다(단기 1시간 · 중기 12시간). 캐시하는 것은 자체 판정 입력값이며 공사 데이터가 아니므로 무저장 원칙과 무관하다.</td>
<td>S</td>
</tr>
<tr>
<td>EI-WX-008</td>
<td>**발표 시각이 지난 발표분만 조회해야 한다.** 중기육상예보 응답에는 `tmFc` 가 들어 있지 않아 어느 발표분을 받았는지 확인할 방법이 없고, 아직 발표되지 않은 `tmFc` 를 요청하면 이전 발표분의 값이 오류 없이 돌아온다(2026.08.26 17:19 · 17:20 실측 — 같은 `tmFc` 로 1분 간격 두 호출이 서로 다른 값을 반환했고 앞의 것은 직전 발표분과 동일했다). 발표분을 하루 밀려 읽으면 강수확률이 통째로 하루씩 어긋난다. 단기예보는 항목마다 `baseDate` · `baseTime` 을 돌려주므로 받은 발표분을 검증할 수 있다.</td>
<td>M</td>
</tr>
</table>
---
## 6. LLM API (EI-LM)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EI-LM-001</td>
<td>LLM 사용은 **두 가지로 한정한다** — ① F01 자연어 일정 구조화 ② F03 운영정보 정규화(사전 파서 실패 조각만). 등급 · 점수 · 충돌 판정에 사용하지 않는다. (SC-FN-007 · PM-SY-007)

**finding 설명 문장 생성은 쓰지 않기로 확정했다** (2026.08.22 · 이슈 #6). 이 조항이 원래 허용했으나 회귀가 막는다 — 결정론성 테스트가 3회 검수의 `message` 완전 일치를 보는데(NF-MT-001), 검수 시점에 LLM 이 문장을 만들면 같은 입력에 다른 문장이 나온다. `finding.message` 는 불변 저장이라 되돌릴 수도 없다. 구현상 `LlmPurpose` 는 `STRUCTURE` · `NORMALIZE` 둘뿐이다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-LM-002</td>
<td>정규화 호출은 **스키마 고정 JSON 출력**으로 수행해야 한다. 출력은 스키마 검증을 거치고 불일치 시 확인 불가로 강등한다. (FR-AU-005 · 006)<br/>**`temperature` 지정을 요구하지 않는다 (개정 2026.09.10)** — 종전 문구는 `temperature 0`을 함께 요구했으나 **Claude 5 계열이 이 파라미터를 폐기해** 지정하면 HTTP 400이 난다. 결정론(NF-MT-001)은 이 값이 아니라 `llm_parse_cache`가 조각 단위로 답을 붙잡아 지키며, 사전 파서 주석도 「`temperature 0`도 매번 같은 답을 보장하지 않는다」고 적고 있었다. 정답셋 13조각 실측에서 이 값 없이도 지어내는 일은 없었고 분량 강제(`MAX_FIXED_CLOSED`)는 그대로다 (이슈 #371)</td>
<td>M</td>
</tr>
<tr>
<td>EI-LM-003</td>
<td>호출 실패 · 타임아웃 시 1회 재시도 후 해당 조각을 확인 불가로 확정하고 검수를 계속한다. (FR-AU-010)</td>
<td>M</td>
</tr>
<tr>
<td>EI-LM-004</td>
<td>LLM에 전송하는 데이터는 해석 대상 원문 조각과 일정 텍스트로 한정한다. 계정 정보 · 인증키 · 타 상품 데이터를 프롬프트에 포함하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EI-LM-005</td>
<td>사전 파서가 처리한 조각에는 LLM을 호출하지 않는다. 커버리지 90% 달성 시 LLM 호출은 조각의 10% 이하여야 한다. (FR-AU-003)</td>
<td>M</td>
</tr>
<tr>
<td>EI-LM-006</td>
<td>LLM 제공자 · 모델명은 환경변수로 관리해 교체 가능해야 한다. 특정 모델의 응답 형식에 파서를 결합하지 않는다.</td>
<td>S</td>
</tr>
</table>
---
## 7. 연동 상태 요약과 남은 작업
<table fit-page-width="true" header-row="true">
<tr>
<td>연동</td>
<td>키 · 승인</td>
<td>실호출 검증</td>
<td>남은 작업</td>
</tr>
<tr>
<td>공사 OpenAPI</td>
<td>✅ 개발계정 발급 완료</td>
<td>✅ 9종 전부</td>
<td>운영계정 신청 (정식 배포 후, W4)</td>
</tr>
<tr>
<td>카카오모빌리티</td>
<td>✅ 앱 생성 · REST 키 발급 완료 (2026.08.13)</td>
<td>✅ 2종 (현재 · 미래)</td>
<td>노출된 키 재발급 권장 · 배포 도메인 등록 (W4)</td>
</tr>
<tr>
<td>기상청 예보</td>
<td>⬜ 공공데이터포털 활용신청 (자동승인)</td>
<td>⬜</td>
<td>단기 · 중기 2건 신청 후 실호출 1회 확인 (W3)</td>
</tr>
<tr>
<td>평년 강수일수</td>
<td>– (로그인 후 다운로드)</td>
<td>–</td>
<td>시도 × 12개월 테이블 구축 (W3)</td>
</tr>
<tr>
<td>LLM</td>
<td>⬜ 제공자 선정 · 키 발급</td>
<td>⬜</td>
<td>W2 파서 프로토타입과 함께 확정</td>
</tr>
</table>
---
## 8. 요구사항 추적
<table fit-page-width="true" header-row="true">
<tr>
<td>연동 영역</td>
<td>연동 ID</td>
<td>연결 요구사항</td>
</tr>
<tr>
<td>공통 정책</td>
<td>EI-CM-001 – 010</td>
<td>PM-SC-001 – 003, FR-AU-027 – 028, FR-OP-001, UI-ST-004, SC-CM-001</td>
</tr>
<tr>
<td>공사 OpenAPI</td>
<td>EI-KT-001 – 008 · 010 – 021</td>
<td>SC-DT-001 – 013, FR-IN-005 – 006 · 020 – 030, FR-AU-001 – 017, FR-RU-010 – 122, FR-MO-001 – 020, FR-OP-001 – 009</td>
</tr>
<tr>
<td>카카오모빌리티</td>
<td>EI-KM-001 – 009</td>
<td>FR-RU-080 – 086, FR-PA-040(이동 지표), SC-DT-010</td>
</tr>
<tr>
<td>기상청</td>
<td>EI-WX-001 – 008</td>
<td>FR-RU-090 – 094, SC-DT-010 · 014</td>
</tr>
<tr>
<td>LLM</td>
<td>EI-LM-001 – 006</td>
<td>FR-IN-003, FR-AU-003 – 010, SC-FN-007, PM-SY-007</td>
</tr>
</table>
---
<callout icon="📌" color="gray_bg">
	**개정 이력**
	v1.0 (2026.08.13) — 최초 작성. 연동 요구사항 48건(EI-CM 10 · EI-KT 16 · EI-KM 9 · EI-WX 7 · EI-LM 6 — EI-KT-009는 결번), 공사 오퍼레이션 계약 9종, 소개정보 필드 분기표 7행, 실측 확정 동작 8건, 카카오모빌리티 실호출 검증 기록 포함. 전 계약은 2026.08.05 – 13 실호출 결과에 근거.
	v1.1 (2026.08.19) — 상품 범위 확대(팀 합의 2026.08.19) 반영. 3-5 호출량 계약에 2박 3일(12곳 · 약 43콜) 시나리오 추가, EI-KM-006 구간 수 병기(12곳 = 11콜), 5-1 강수 판정 기준일을 여행 일자 기준으로 명확화(FR-RU-091 정합). 사용 오퍼레이션 9종 구성은 변경 없음.
	v1.2 (2026.08.20) — D0 재실호출(48콜) 결과 반영. ① EI-KT-011 — 동기화 목록 건수가 조회 시각에 따라 크게 변동함을 명시(새벽 11건 / 오후 177건) ② EI-KT-014 — 시도 목록 변동 사례(`12 전남광주통합특별시` 신설) 추가 ③ EI-KT-018 · 019 · 020 신설 — 상세 조회의 구 코드체계 전부 빈값 · 문의처의 기관명+전화 혼재 · 문화시설 `spendtime` 빈값 ④ 소개정보 분기표의 레포츠 행에 입실/퇴실 패턴 경고 병기. 연동 요구사항 48건 → 51건.
	v1.3 (2026.08.24) — R09 강수 판정 분기를 실측에 맞춰 정정. 중기육상예보가 **발표일 +5일부터** 시작해 `rnSt3` · `rnSt4` 필드가 없다(실호출 확인). D+3 을 중기로 보내면 빈 값을 받고 그걸 강수확률 0 으로 읽어 비 오는 날을 정상 판정하게 된다. 구간을 D+0–D+2 단기 / **D+3 단기 부분 커버리지** / **D+4–D+10 중기** / D+11 이상 평년 으로 바꾸고, 중기는 최신 발표분이 아니라 **대상 일자를 덮는 발표분**을 고르도록 했다. EI-WX-001 의 「공사 인증키와 별개의 키」 서술도 정정 — data.go.kr 은 계정당 키 하나이고 활용신청만 서비스별이다. 관련 이슈 #54 · #55.
	v1.4 (2026.08.24) — 구현 실측 대조. ① 백엔드를 Spring Boot 3 → **NestJS 10 + TypeScript** 로 정정. 아키텍처 다이어그램과 기술 스택 표가 실제와 달랐고, 기획서는 기능설명서의 원본이라 그대로 두면 제출 서류와 구동 코드가 어긋난다. 선택 근거(상시 구동 · 공용 상수 단일 출처 · eslint 로 강제하는 결정론성)도 함께 적었다 ② LLM 허용 용도를 셋 → **둘**로 정정 — finding 설명문 생성은 결정론성 때문에 쓰지 않기로 확정했고 구현의 `LlmPurpose` 도 `STRUCTURE` · `NORMALIZE` 둘뿐이다 ③ 배치를 Spring Scheduler → Node 프로세스 내 스케줄러, 배포를 Railway 로 구체화.
	v1.5 (2026.08.26) — 중기육상예보 응답 범위를 실측에 맞춰 재정정(이슈 #92). v1.3 이 「발표일 +5 ~ +10」 로 단정했는데 **06시 발표는 +4 부터** 준다. 어제 18시 발표의 `rnSt5Am` 과 오늘 06시 발표의 `rnSt4Am` 이 60 · 「흐리고 비」 로 같아 둘 다 08-30 임을 확인했다. v1.3 이 적어 둔 우회(「D+4 는 어제 발표분으로 닿는다」)는 필요 없고, 대신 저녁의 최신 발표분(18시)이 D+4 를 잃는다. EI-WX-008 신설 — 중기 응답에 `tmFc` 가 없어 발표 전 `tmFc` 를 물으면 이전 발표분 값을 받고도 모른다. 5-1 D+3 행에 실측 커버리지(D+3 21시까지) 병기. 연동 요구사항 51건 → 52건.
	v1.7 (2026.09.10) — EI-LM-002 에서 `temperature 0` 요구를 뺐다. **Claude 5 계열이 이 파라미터를 폐기해** 지정하면 HTTP 400 이 나고, 그 상태로는 신형 모델을 쓸 수 없다. 결정론(NF-MT-001)은 이 값이 아니라 `llm_parse_cache` 가 조각 단위로 답을 붙잡아 지킨다 — 사전 파서 주석도 「`temperature 0` 도 매번 같은 답을 보장하지 않는다」고 이미 적고 있었다. 정답셋(`operating_info.csv` 287건)에서 뽑은 조각 13종으로 두 모델을 이 값 없이 2회씩 돌려 확인했다 — 지어냄 0, 스키마 이탈 0. 분량 강제(`MAX_FIXED_CLOSED`)는 그대로다. 어댑터와 그 스펙을 함께 고쳤다. 이슈 #371.
	v1.6 (2026.08.28) — T1 조회 설계에서 확정. **EI-KT-021 신설** — `areaBasedList2`의 `arrange` 동작(`D` 생성일 내림차순 · `C` 수정일 내림차순 · 미지정 시 무정렬)을 3콜 프로브로 확정하고, 3-3 계약표의 `areaBasedList2` 행에 `arrange=D`와 `createdtime`을 반영했다. 정렬이 없으면 한 지역 1,005건을 훑어야 해 T1 이 상품당 열 콜을 넘긴다. 연동 요구사항 52건 → 53건.
</callout>
