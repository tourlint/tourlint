<callout icon="🛡️" color="gray_bg">
	**TourLint 예외처리 요구사항**
	이 문서는 앞선 7개 요구사항 문서에 흩어져 있는 **실패 모드를 한곳에 모아 처리 방식을 확정**한다.
	TourLint에서 예외 처리는 부가 기능이 아니다. **"모르는 것은 모른다고 말한다"** 는 설계 원칙 자체가 예외를 산출물로 바꾸는 장치이기 때문이다. 확인 불가 등급과 확인 필요 목록은 예외 처리의 결과물이지 오류가 아니다.
</callout>
<table fit-page-width="true" header-column="true">
<tr>
<td>문서</td>
<td>통합 요구사항 명세서 – 예외처리 요구사항</td>
</tr>
<tr>
<td>버전</td>
<td>v1.8</td>
</tr>
<tr>
<td>기준일</td>
<td>2026.08.14</td>
</tr>
<tr>
<td>선행 문서</td>
<td>용어 정의 · 서비스 범위 · 기능 · 화면 · 권한 · 외부 연동 · 데이터 · 비기능 요구사항 (전 8종)</td>
</tr>
<tr>
<td>성격</td>
<td>선행 문서의 실패 모드 합집합. 신규 정책이 아니라 확정과 상세화</td>
</tr>
</table>
<table_of_contents/>
---
## 1. 문서 개요
### 1-1. ID 체계
<table fit-page-width="true" header-row="true">
<tr>
<td>접두어</td>
<td>영역</td>
</tr>
<tr>
<td>`EX-CM`</td>
<td>공통 원칙 · 분류 · 사유 코드</td>
</tr>
<tr>
<td>`EX-IN`</td>
<td>입력 · 업로드 · 구조화</td>
</tr>
<tr>
<td>`EX-MC`</td>
<td>관광지 매칭</td>
</tr>
<tr>
<td>`EX-PS`</td>
<td>운영정보 해석기</td>
</tr>
<tr>
<td>`EX-EI`</td>
<td>외부 연동</td>
</tr>
<tr>
<td>`EX-AU`</td>
<td>검수 실행</td>
</tr>
<tr>
<td>`EX-PA`</td>
<td>패치 · 되돌리기</td>
</tr>
<tr>
<td>`EX-MO`</td>
<td>감시 · 배치</td>
</tr>
<tr>
<td>`EX-QT`</td>
<td>쿼터 · 예산</td>
</tr>
<tr>
<td>`EX-PL`</td>
<td>기획 화면 (장소 찾기 · 장소 정보 한 줄 · 장소 담기 · 검수 시작)</td>
</tr>
<tr>
<td>`EX-AG`</td>
<td>AI 에이전트 (기획 · 검수 · 레이더)</td>
</tr>
<tr>
<td>`EX-SY`</td>
<td>시스템 · 인증 · 권한</td>
</tr>
<tr>
<td>`EX-MS`</td>
<td>사용자 메시지 규칙</td>
</tr>
</table>
---
## 2. 예외 처리 3원칙 (EX-CM)
<table fit-page-width="true" header-row="true">
<tr>
<td>원칙</td>
<td>내용</td>
<td>귀결</td>
</tr>
<tr>
<td>**① 부분 성공**</td>
<td>실패는 가능한 가장 작은 단위로 격리한다. 한 콘텐츠 조회가 실패해도 나머지는 정상 판정한다</td>
<td>전체 중단 금지. 화면은 항상 살아 있다</td>
</tr>
<tr>
<td>**② 모르면 모른다**</td>
<td>판단 근거가 없으면 정상으로 넘기지 않고 확인 불가로 명시한다</td>
<td>예외가 확인 필요 목록이라는 산출물이 된다</td>
</tr>
<tr>
<td>**③ 사용자 데이터 불변**</td>
<td>어떤 예외 상황에서도 시스템이 사용자 일정을 자동으로 바꾸거나 지우지 않는다</td>
<td>복구는 항상 사용자 확인을 거친다</td>
</tr>
</table>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EX-CM-001</td>
<td>모든 예외는 **처리 단위를 명시**해야 한다 — 조각 · 구간 · 콘텐츠 · 규칙 · 일정 항목 · 상품 · 요청 · 배치 8종 중 하나다. 단위를 정하지 않은 예외 처리를 두지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-002</td>
<td>모든 예외는 **예외 사유 코드**를 가져야 하며, 코드는 4장 목록에 정의된 값이어야 한다. 이는 **규칙 판정 사유코드**(`REST_DAY_CONFLICT` 등, 데이터 요구사항 5-5절)와 네임스페이스가 다르며, 둘 다 `finding.reason_code`에 기록될 수 있다. 코드 없는 오류를 사용자에게 노출하지 않는다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-003</td>
<td>예외로 인해 판정이 불가능해진 항목은 **삭제하거나 건너뛰지 않고** 확인 불가 등급으로 결과에 남겨야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-004</td>
<td>예외 발생 시 사용자에게는 **원인과 다음 행동**을 함께 안내해야 한다. 코드값만 표시하지 않는다. (NF-US-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-005</td>
<td>예외 로그에는 사유 코드 · 처리 단위 · 대상 식별자 · 발생 시각을 남겨야 하며, 인증키 · 비밀번호 · 세션 토큰을 포함하지 않아야 한다. (NF-OB-005 · 006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-006</td>
<td>사용자 화면과 API 응답에 스택 트레이스 · 쿼리문 · 내부 경로를 노출하지 않아야 한다. (NF-SC-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-007</td>
<td>예외 처리 로직은 판정 결정론성을 깨뜨리지 않아야 한다. 동일 입력 · 동일 실패 상황이면 동일한 사유 코드와 등급이 나와야 한다. (NF-MT-001)</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-008</td>
<td>재시도는 **멱등한 조회 요청에만** 적용한다. 패치 확정 · 출시 승인 같은 상태 변경 요청은 자동 재시도하지 않는다.</td>
<td>M</td>
</tr>
</table>
---
## 3. 예외 분류 체계
```plain text
        영향 범위                     처리 방식
  ┌───────────────────┐        ┌────────────────────────┐
  │ 조각 (fragment)   │  ───→  │ unparsed 기록 · 해당 필드만 확인 불가
  │ 구간 (segment)    │  ───→  │ 해당 구간만 확인 불가 (R08) · 타 구간 정상 판정
  │ 콘텐츠 (content)  │  ───→  │ 해당 콘텐츠만 확인 불가 · 나머지 정상 판정
  │ 규칙 (rule)       │  ───→  │ 해당 규칙만 확인 불가 · 타 규칙 정상 수행
  │ 일정 항목 (item)  │  ───→  │ 항목 유지 + "직접 정한 곳" 표시 · 감점 없음
  │ 상품 (product)    │  ───→  │ 부분 검수 표시 · 점수 미산출
  │ 요청 (request)    │  ───→  │ 거부 + 사유 안내 · 데이터 변경 없음
  │ 배치 (batch)      │  ───→  │ 실패 기록 · last_covered 미갱신 · 다음 회차 복구
  └───────────────────┘        └────────────────────────┘

원칙: 위에서 아래로 갈수록 영향이 크다.
      예외는 가능한 한 위쪽 단위로 격리하고, 아래로 확대하지 않는다.
```
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EX-CM-010</td>
<td>예외는 발생 지점에서 가장 작은 단위로 격리해야 하며, 상위 단위로 승격하는 조건을 명시적으로 정의한 경우에만 확대할 수 있다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-011</td>
<td>콘텐츠 단위 실패가 **전체의 50%를 초과**하면 상품 단위로 승격해 부분 검수로 처리한다. 이것이 **상품 단위로의 유일한 자동 승격 조건**이다. 구간 → 규칙 승격(EX-EI-022)과 같은 하위 단계 확대는 개별 조항에 명시된 경우에 한해 허용한다. (FR-AU-029)</td>
<td>M</td>
</tr>
</table>
---
## 4. 사유 코드 목록
<callout icon="🔖" color="blue_bg">
	사유 코드는 로그 · API 응답 · finding에 기록하며 **화면에는 직접 노출하지 않는다.** 화면에는 코드에 대응하는 한국어 안내 문구를 표시한다. (G-015)
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>코드</td>
<td>단위</td>
<td>의미</td>
<td>결과 등급</td>
</tr>
<tr>
<td>`UPLOAD_FORMAT_INVALID`</td>
<td>요청</td>
<td>업로드 양식 불일치 · 필수 컬럼 누락</td>
<td>거부</td>
</tr>
<tr>
<td>`UPLOAD_ROW_INVALID`</td>
<td>요청</td>
<td>일부 행의 날짜 · 시각 형식 오류</td>
<td>부분 수용</td>
</tr>
<tr>
<td>`UPLOAD_LIMIT_EXCEEDED`</td>
<td>요청</td>
<td>파일 크기 · 행수 상한 초과</td>
<td>거부</td>
</tr>
<tr>
<td>`DAY_COUNT_MISMATCH`</td>
<td>요청</td>
<td>박수와 일자별 일정 수 불일치</td>
<td>거부</td>
</tr>
<tr>
<td>`NL_STRUCTURE_FAILED`</td>
<td>요청</td>
<td>자연어 구조화 실패</td>
<td>거부 + 직접 입력 유도</td>
</tr>
<tr>
<td>`PLACE_NOT_FOUND`</td>
<td>일정 항목</td>
<td>키워드 검색 결과 0건</td>
<td>아직 고르지 않은 장소로 유지 (사용자가 고를 때만 검수 제외)</td>
</tr>
<tr>
<td>`PLACE_UNRESOLVED`</td>
<td>상품</td>
<td>동명 후보 미확정 상태로 검수 시도</td>
<td>실행 차단</td>
</tr>
<tr>
<td>`PARSE_MISSING`</td>
<td>조각</td>
<td>필드 자체가 빈 문자열</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`PARSE_TARGET_VARIES`</td>
<td>조각</td>
<td>대상별 상이 표현</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`PARSE_REFERENCE`</td>
<td>조각</td>
<td>홈페이지 참조 · 문의 요망</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`PARSE_CONDITIONAL`</td>
<td>조각</td>
<td>조건부이나 구조화 실패 · 조각 간 시간 충돌</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`PARSE_SCHEMA_INVALID`</td>
<td>조각</td>
<td>LLM 출력이 스키마 검증 실패</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`LLM_UNAVAILABLE`</td>
<td>조각 · 요청(에이전트)</td>
<td>LLM 호출 실패 · 타임아웃 (에이전트는 시간 상한 30초 초과 포함)</td>
<td>확인 불가 (조각) · 에이전트는 거절하지 않고 끝난 항목만 돌려준다(200 `incomplete`), 사람이 하는 길 유지</td>
</tr>
<tr>
<td>`KTO_FETCH_FAILED`</td>
<td>콘텐츠</td>
<td>공사 상세 조회 실패 (재시도 소진)</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`CONTENT_NOT_FOUND`</td>
<td>콘텐츠</td>
<td>상세 조회가 HTTP·`resultCode` 상 성공이나 항목이 0건 (삭제된 `contentid` 등)</td>
<td>확인 불가</td>
</tr>
<tr>
<td>`KTO_AUTH_ERROR`</td>
<td>요청</td>
<td>인증키 오류 · 미등록</td>
<td>실행 차단 + 운영 알림</td>
</tr>
<tr>
<td>`KTO_QUOTA_EXCEEDED`</td>
<td>요청</td>
<td>공사 일일 한도 초과</td>
<td>실행 차단</td>
</tr>
<tr>
<td>`CONTENT_HIDDEN`</td>
<td>콘텐츠</td>
<td>`showflag`가 0으로 전환</td>
<td>차단</td>
</tr>
<tr>
<td>`COORD_MISSING`</td>
<td>일정 항목</td>
<td>좌표 결측으로 이동시간 산출 불가</td>
<td>확인 불가 (R08)</td>
</tr>
<tr>
<td>`ROUTE_NOT_FOUND`</td>
<td>구간</td>
<td>지도 API가 경로를 찾지 못함</td>
<td>확인 불가 (R08)</td>
</tr>
<tr>
<td>`ROUTE_PROVIDER_FAILED`</td>
<td>규칙</td>
<td>지도 API 호출 실패</td>
<td>확인 불가 (R08)</td>
</tr>
<tr>
<td>`TRANSIT_NOT_SUPPORTED`</td>
<td>구간</td>
<td>이동수단이 대중교통</td>
<td>확인 불가 (R08)</td>
</tr>
<tr>
<td>`FORECAST_UNAVAILABLE`</td>
<td>규칙</td>
<td>예보 API 실패 · 평년 테이블로 강등</td>
<td>주의 (근거 표기 변경)</td>
</tr>
<tr>
<td>`CLIMATE_DATA_MISSING`</td>
<td>규칙</td>
<td>해당 지역 · 월의 평년 데이터 없음</td>
<td>확인 불가 (R09)</td>
</tr>
<tr>
<td>`AUDIT_PARTIAL`</td>
<td>상품</td>
<td>실패 콘텐츠 50% 초과</td>
<td>부분 검수 · 점수 미산출</td>
</tr>
<tr>
<td>`AUDIT_TIMEOUT`</td>
<td>상품</td>
<td>검수 작업 30분 초과</td>
<td>실패 확정 · 재실행 가능</td>
</tr>
<tr>
<td>`DISMISS_REASON_REQUIRED`</td>
<td>요청</td>
<td>finding 무시 요청에 사유 없음</td>
<td>거부</td>
</tr>
<tr>
<td>`PATCH_CONFLICT`</td>
<td>요청</td>
<td>수정안 간 충돌 검출</td>
<td>확정 차단</td>
</tr>
<tr>
<td>`PATCH_STALE`</td>
<td>요청</td>
<td>확정 시점에 일정이 이미 변경됨</td>
<td>거부 + 재검토 유도</td>
</tr>
<tr>
<td>`UNDO_UNAVAILABLE`</td>
<td>요청</td>
<td>되돌릴 패치 없음 · 1단계 초과</td>
<td>거부</td>
</tr>
<tr>
<td>`FINGERPRINT_INCOMPARABLE`</td>
<td>콘텐츠</td>
<td>지문의 판정 필드 목록이 달라 비교 불가</td>
<td>재판정 · 알림 없음</td>
</tr>
<tr>
<td>`BATCH_EMPTY`</td>
<td>배치</td>
<td>평일 조회 결과 0건 (동기화 지연 의심)</td>
<td>`last_covered` 미갱신</td>
</tr>
<tr>
<td>`BATCH_HIDDEN_OVERFLOW`</td>
<td>배치</td>
<td>비표출 전환이 페이지 상한 초과</td>
<td>개별 확인으로 전환</td>
</tr>
<tr>
<td>`BUDGET_THRESHOLD`</td>
<td>배치</td>
<td>예산 소진율 80% 이상</td>
<td>배치 자동 중지</td>
</tr>
<tr>
<td>`BUDGET_EXHAUSTED`</td>
<td>요청</td>
<td>예산 소진율 100% (기획 조회 · 에이전트의 공사 호출 포함)</td>
<td>신규 검수 · 기획 조회 · 에이전트 차단. 일정 입력 · 저장은 계속된다. 에이전트가 실행 중에 막히면 끝난 항목만 돌려준다(200 `incomplete`)</td>
</tr>
<tr>
<td>`RATE_LIMIT_EXCEEDED`</td>
<td>요청</td>
<td>호출 빈도 상한 초과(검수 실행 · 리포트 생성 · 기획 조회 · 에이전트, API 3-4) 또는 같은 계정의 같은 에이전트가 도는 중에 다시 요청</td>
<td>거부 · 새로 실행하지 않음 · 잠시 후 다시</td>
</tr>
<tr>
<td>`FORBIDDEN_ACTION`</td>
<td>요청</td>
<td>전면 금지 동작 시도 (차단 무시 · 출시 승인 등)</td>
<td>거부</td>
</tr>
<tr>
<td>`NOT_AUTHENTICATED`</td>
<td>요청</td>
<td>미인증 · 세션 만료</td>
<td>거부 + 재인증</td>
</tr>
<tr>
<td>`NOT_FOUND`</td>
<td>요청</td>
<td>타 계정 리소스 접근 · 존재하지 않는 리소스</td>
<td>거부 (존재 여부 비노출)</td>
</tr>
<tr>
<td>`REPORT_FAILED`</td>
<td>요청</td>
<td>PDF 생성 실패</td>
<td>거부 + 재시도 유도</td>
</tr>
<tr>
<td>`SETTING_NOT_STRICTER`</td>
<td>요청</td>
<td>회사 기준이 표준보다 느슨함 (연속 일정 기준 6시간 초과 · 최소 식사 시간 60분 미만)</td>
<td>거부 · 저장값 유지</td>
</tr>
<tr>
<td>`INPUT_INVALID`</td>
<td>요청</td>
<td>입력 형식 오류 — 전용 코드가 없는 요청 값 오류(허용 값 밖 · 빠진 필수 값 · 깨진 JSON 본문)</td>
<td>거부</td>
</tr>
<tr>
<td>`INTERNAL_ERROR`</td>
<td>요청</td>
<td>분류되지 않은 서버 오류</td>
<td>거부 + 로그 기록</td>
</tr>
</table>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EX-CM-020</td>
<td>사유 코드는 코드 상수로 정의하고 문자열 하드코딩을 금지한다. 신규 코드 추가 시 본 문서를 함께 개정한다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-CM-021</td>
<td>`INTERNAL_ERROR`는 최후 수단이다. 개발 기간 중 이 코드가 기록되면 원인을 분류해 전용 코드를 신설해야 한다.</td>
<td>S</td>
</tr>
<tr>
<td>EX-CM-022</td>
<td>규칙 판정 사유코드는 아래 「규칙 판정 사유코드 목록」의 값만 사용하며, 신설 시 본 문서를 함께 개정한다. `finding.reason_code`에는 판정 사유코드 또는 예외 사유코드가 기록된다.</td>
<td>M</td>
</tr>
</table>
---
### 규칙 판정 사유코드 목록 (EX-CM-022)
판정 finding의 `reason_code`에 기록하는 코드로, 위 예외 사유 코드와 네임스페이스가 분리된다. 화면에는 직접 노출하지 않는다(G-015). 아래 15종 외에는 새 코드를 만들지 않고 예외 사유코드를 그대로 기록한다 — R05는 `PARSE_*` · `KTO_FETCH_FAILED` · `COORD_MISSING`, R06 비표출 전환은 `CONTENT_HIDDEN`, R08 외부 실패는 `ROUTE_NOT_FOUND` · `ROUTE_PROVIDER_FAILED` · `TRANSIT_NOT_SUPPORTED`, R09 강등 · 결측은 `FORECAST_UNAVAILABLE` · `CLIMATE_DATA_MISSING`.
<table fit-page-width="true" header-row="true">
<tr>
<td>코드</td>
<td>생성 주체</td>
<td>결과 등급</td>
<td>의미</td>
</tr>
<tr>
<td>`REST_DAY_CONFLICT`</td>
<td>R01</td>
<td>차단</td>
<td>휴무일에 방문 일정 (기존)</td>
</tr>
<tr>
<td>`OPEN_HOUR_CONFLICT`</td>
<td>R01</td>
<td>차단</td>
<td>운영시간 밖 방문 (기존)</td>
</tr>
<tr>
<td>`ADMISSION_CUTOFF`</td>
<td>R01</td>
<td>오류</td>
<td>입장 마감 이후 방문 (기존)</td>
</tr>
<tr>
<td>`IN_BREAK_TIME`</td>
<td>R01</td>
<td>주의</td>
<td>휴게 · 준비시간 구간 방문 (기존)</td>
</tr>
<tr>
<td>`REST_DAY_UNCERTAIN`</td>
<td>R01</td>
<td>주의 + 확인 필요 목록</td>
<td>조건부 휴무 가능성 — CLOSED_UNCERTAIN · 신뢰도 추정 (FR-AU-008 · 데이터 5-5, 신설)</td>
</tr>
<tr>
<td>`EVENT_ENDED`</td>
<td>R02</td>
<td>차단</td>
<td>방문일 이전에 행사 종료 (신설)</td>
</tr>
<tr>
<td>`EVENT_NOT_STARTED`</td>
<td>R02</td>
<td>차단</td>
<td>방문일에 행사 미개시 (신설)</td>
</tr>
<tr>
<td>`TIME_OVERLAP`</td>
<td>R03</td>
<td>오류</td>
<td>같은 일차 내 일정 시간 중복 (신설)</td>
</tr>
<tr>
<td>`CONTENT_IMBALANCE`</td>
<td>R04</td>
<td>주의</td>
<td>동일 유형 콘텐츠가 임계치 이상 반복 (신설)</td>
</tr>
<tr>
<td>`MEAL_REST_MISSING`</td>
<td>R07</td>
<td>주의</td>
<td>연속 일정 기준 시간 내 식사 · 휴식 없음 — FR-RU-070 (신설)</td>
</tr>
<tr>
<td>`MEAL_TIME_SHORT`</td>
<td>R07</td>
<td>주의</td>
<td>식사 배정 시간이 최소 기준 미만 — FR-RU-071 (신설)</td>
</tr>
<tr>
<td>`TRAVEL_TIME_SHORT`</td>
<td>R08</td>
<td>오류</td>
<td>배정된 이동 가능 시간이 예상 이동시간보다 부족 (신설)</td>
</tr>
<tr>
<td>`RAIN_RISK`</td>
<td>R09</td>
<td>주의</td>
<td>야외 비중 · 강수 지표 임계 동시 초과 (신설)</td>
</tr>
<tr>
<td>`TARGET_MISMATCH`</td>
<td>R10</td>
<td>주의</td>
<td>기대 콘텐츠 프로파일 결손 유형 존재 (신설)</td>
</tr>
<tr>
<td>`PRE_DEPARTURE_CHECK`</td>
<td>F07 (FR-AU-085)</td>
<td>확인 필요 목록 전용 · 감점 제외</td>
<td>출발 1일 이내 상품에 자동 생성되는 "출발 전 운영기관 최종 확인" 항목 — D+1 시차 대응 (신설 · API 5-7 기사용분 소급 정의)</td>
</tr>
</table>
<callout icon="🔀" color="yellow_bg">
	**네임스페이스 구분** — 예외 사유 코드 표의 `PARSE_*` 예외 사유 코드는 정규화 결과의 `unparsed[].reason` 값과 **별개**다. 저장되는 값은 데이터 요구사항 5-3절의 접두어 없는 열거값이며, 로그와 finding에 기록하는 것이 `PARSE_*` 코드다.
	대응 관계 — `PARSE_MISSING` ↔ `MISSING` · `PARSE_TARGET_VARIES` ↔ `TARGET_VARIES` · `PARSE_REFERENCE` ↔ `REFERENCE` · `PARSE_CONDITIONAL` ↔ `CONDITIONAL` · `PARSE_SCHEMA_INVALID` ↔ `SCHEMA_INVALID` · `LLM_UNAVAILABLE`은 양쪽 동일.
</callout>
---
## 5. 입력과 업로드 (EX-IN)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-IN-001</td>
<td>업로드 파일이 지정 양식과 다름 · 필수 컬럼 누락</td>
<td>전체 거부. 어떤 컬럼이 없는지 명시하고 양식 파일 내려받기 링크를 함께 제공한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-002</td>
<td>일부 행의 날짜 · 시각 형식 오류</td>
<td>`UPLOAD_ROW_INVALID` — **정상 행은 유지하고 실패 행만 표시**한다. 행 번호와 사유를 함께 보여주며 화면에서 수정할 수 있게 한다 (FR-IN-015)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-003</td>
<td>파일 크기 5MB 또는 500행 초과</td>
<td>`UPLOAD_LIMIT_EXCEEDED` — 파싱 전에 거부. 상한값을 안내한다 (NF-CP-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-004</td>
<td>유효 일정 항목이 45건 초과</td>
<td>`UPLOAD_LIMIT_EXCEEDED` — 저장 거부. 상품을 나누도록 안내한다 (NF-CP-003 · NF-CP-010)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-005</td>
<td>박수와 일자별 일정 수 불일치</td>
<td>`DAY_COUNT_MISMATCH` — **검수 시작 거부**. 어느 일차가 비었는지 표시한다. **저장(기획 중)은 빈 일차·빈 일정을 허용하고**(빈 상품 즉시 생성 — 상품을 일찍 만들고 항목을 이어서 채운다), 박수↔일정 완성도는 검수 시작(handoff)이 검사한다 (FR-IN-016 · FR-PL-020)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-006</td>
<td>자연어 구조화가 일정 항목을 하나도 만들지 못함</td>
<td>`NL_STRUCTURE_FAILED` — 거부하고 직접 입력 또는 양식 업로드로 유도한다. 빈 상품을 생성하지 않는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-007</td>
<td>자연어 구조화가 일부만 성공</td>
<td>성공분을 편집 가능한 상태로 제시하고, 원문에서 해석하지 못한 부분을 함께 표시한다. 사용자 확인 없이 저장하지 않는다 (FR-IN-013)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-008</td>
<td>자연어 입력 5,000자 초과</td>
<td>거부. 상한을 안내한다 (NF-CP-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-009</td>
<td>일정 항목의 종료시간 미입력</td>
<td>예외가 아니다. 중분류별 기본 체류시간으로 보완하고 `기본값 적용` 배지를 표시한다 (FR-IN-011)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-010</td>
<td>지역 코드 조회(`ldongCode2`) 실패로 지역 선택 불가</td>
<td>지역 드롭다운을 비활성화하고 재시도 수단을 제공한다. 하드코딩 목록으로 대체하지 않는다 (NF-MT-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-011</td>
<td>업로드 파일이 파싱 중 서버 오류를 유발</td>
<td>`UPLOAD_FORMAT_INVALID`(400) — 파일을 저장하지 않고 폐기한 뒤 거부한다. 부분 저장 상태를 남기지 않는다. 이름만 `.xlsx` 로 바꾼 파일처럼 내용이 형식과 다르면 파서에 넘기기 전에 거부한다 (NF-SC-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-IN-012</td>
<td>저장 요청이 입력 검증에 걸려 거절됨</td>
<td>`INPUT_INVALID` — **서버가 적어 보낸 이유를 화면이 그대로 보인다.** 「잠시 후 다시 시도해 주세요」로 갈음하지 않는다 — 다시 눌러도 같기 때문이다. 어느 일차 · 몇 번째 줄의 무엇이 비었는지까지 말한다. 화면은 같은 것을 **보내기 전에** 먼저 보고, 아무것도 채우지 않은 줄(추가만 하고 만 줄)은 버린 뒤 남은 줄만 검사한다.</td>
<td>M</td>
</tr>
</table>
---
## 6. 관광지 매칭 (EX-MC)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-MC-001</td>
<td>키워드 검색 결과 0건</td>
<td>`PLACE_NOT_FOUND` — 일정 항목을 **삭제하지 않고 유지**하되 자동으로 검수 제외하지 않는다. 항목은 아직 고르지 않은 장소(`PENDING`)로 남기고, 장소 칸에는 숫자 대신 "관광정보에 올라 있는 이름으로 검색해 보세요"를 보인다(EX-PL-010). 사용자가 "직접 정한 곳으로 두기"를 고르거나 검수 시작 창에서 "이대로 검수 시작"으로 넘길 때만 직접 정한 곳(`EXCLUDED`)이 되어 전 규칙 판정과 감점 대상에서 빠진다 (FR-IN-024 · 025 · 026)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MC-002</td>
<td>검색 결과가 여러 건인데 사용자가 확정하지 않음</td>
<td>임의로 첫 후보를 선택하지 않고 아직 고르지 않은 장소로 남긴다. 그 상태로 "검수 시작"을 누르면 확인 창에 남은 목록을 보인다(EX-PL-005). 확인 창을 거치지 않은 검수 요청은 EX-AU-001로 거부된다 (UI-S2-023)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MC-003</td>
<td>검색 결과가 10건 이상</td>
<td>상품 지역으로 자동 필터링하고 필터 적용 사실과 해제 수단을 표시한다 (FR-IN-023)</td>
<td>S</td>
</tr>
<tr>
<td>EX-MC-004</td>
<td>키워드 검색 호출 자체가 실패</td>
<td>해당 항목을 미확정(`PENDING`) 상태로 두고 재시도 수단을 제공한다. 검수 제외로 자동 전환하지 않는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-MC-005</td>
<td>확정된 `contentid`가 이후 조회에서 사라짐 (삭제 · 비표출)</td>
<td>`CONTENT_HIDDEN`으로 처리해 차단 등급으로 재판정하고, 정보 카드는 표시하지 않는다. 일정 항목은 유지한다 (FR-RU-065 – 067)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MC-006</td>
<td>확정 콘텐츠에 좌표(`mapx` · `mapy`)가 없음</td>
<td>R08만 `COORD_MISSING`으로 확인 불가 처리한다. 다른 규칙은 정상 수행한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-MC-007</td>
<td>대체 관광지 추천 조회(위치기반 목록)가 실패하거나 0건</td>
<td>해당 수정안만 제시하지 않고 나머지 수정안은 정상 제공한다. finding 자체는 유지한다</td>
<td>M</td>
</tr>
</table>
---
## 7. 운영정보 해석기 (EX-PS)
<callout icon="🧬" color="yellow_bg">
	해석 실패는 **오류가 아니라 정상 산출물**이다. 실측 287건 기준 결측률이 휴무일 16.7% · 운영시간 7.3%이므로, 관광지 8곳 상품에서 확인 불가 1 – 2건은 예상 범위다.
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-PS-001</td>
<td>판정 필드가 빈 문자열</td>
<td>`unparsed[].reason`에 `MISSING`을 기록하고 해당 경로 신뢰도를 `UNPARSED`로 둔다. 예외 사유 코드는 `PARSE_MISSING`이며 R05로 이관한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-002</td>
<td>대상별 상이 · 참조형 표현</td>
<td>`unparsed[].reason`에 `TARGET_VARIES` · `REFERENCE`를 기록하고 예외 사유 코드는 `PARSE_TARGET_VARIES` · `PARSE_REFERENCE`를 사용한다. 정상으로 넘기지 않는다 (FR-AU-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-003</td>
<td>사전 파서 실패 후 LLM 호출도 실패 · 타임아웃</td>
<td>**1회 재시도** 후에도 실패하면 `LLM_UNAVAILABLE`로 확정하고 검수 전체는 계속 진행한다 (FR-AU-010)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-004</td>
<td>LLM 출력이 스키마 검증 실패</td>
<td>`PARSE_SCHEMA_INVALID`로 확인 불가 강등한다. 검증 실패한 값을 저장하지 않는다 (DR-IN-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-005</td>
<td>같은 요일에 서로 다른 운영시간을 주장하는 조각이 둘 이상</td>
<td>해당 요일 항목을 만들지 않고 `unparsed[].reason = CONDITIONAL`로 기록한다(예외 사유 코드 `PARSE_CONDITIONAL`). 임의로 하나를 선택하지 않는다 (DR-NM-013)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-006</td>
<td>`alwaysOpen`과 구체적 휴무가 함께 나옴</td>
<td>예외가 아니라 병합 규칙 적용 대상이다. 전체 휴관이면 `alwaysOpen`을 내리고, 일부 휴관이면 유지한 채 `partialClosed`에 담는다. 두 경우 모두 신뢰도를 `ESTIMATED`로 낮춘다 (DR-NM-011)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-007</td>
<td>운영시간 원문에 시각 범위가 둘 이상인데 라벨이 없음</td>
<td>첫 범위를 운영시간으로 단정하지 않는다. 라벨을 판별할 수 없으면 `PARSE_CONDITIONAL`로 확인 불가 처리한다 (FR-AU-014)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-008</td>
<td>정규화 결과 일부만 신뢰도 `ESTIMATED`</td>
<td>**해당 경로를 쓰는 규칙만** 차단에서 주의로 강등하고 확인 필요 목록에 동시 등록한다. 다른 규칙은 영향받지 않는다 (DR-NM-034)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-009</td>
<td>파싱이 전면 실패해 `normalized_json`을 만들 수 없음</td>
<td>`normalized_json`을 `NULL`로, `parse_confidence`를 `UNPARSED`로 저장한다. 지문은 정상 생성한다 (DR-IN-009 · DR-FP-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PS-010</td>
<td>확인 불가 항목의 문의처도 없음</td>
<td>확인 필요 목록의 문의처 칸에 표준 문구 "정보 없음"을 표시하고 홈페이지 링크가 있으면 대신 제공한다. 항목 자체를 숨기지 않는다 (FR-AU-082)</td>
<td>M</td>
</tr>
</table>
---
## 8. 외부 연동 (EX-EI)
### 8-1. 한국관광공사 OpenAPI
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-EI-001</td>
<td>응답 본문이 꺾쇠괄호로 시작 (XML 오류 응답)</td>
<td>JSON 파싱 오류로 오인하지 않고 **인증 실패 · 쿼터 초과로 분류**한다. 이 서비스에서 가장 흔한 오해 지점이다 (EI-KT-002)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-002</td>
<td>`resultCode`가 인증키 오류 계열</td>
<td>`KTO_AUTH_ERROR`로 처리하고 검수를 즉시 중단한다. 재시도하지 않으며 운영자에게 알린다. 인코딩키 · 디코딩키 혼동이 대표 원인이다</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-003</td>
<td>`resultCode`가 쿼터 초과 계열</td>
<td>`KTO_QUOTA_EXCEEDED`로 처리하고 예산 관리자에 즉시 반영한다. 재시도하지 않는다. 사용자에게 재개 시점을 안내한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-004</td>
<td>`items`가 빈 문자열로 반환 (0건)</td>
<td>정상 응답으로 처리한다. 파싱 오류로 취급하지 않는다 (EI-KT-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-005</td>
<td>`items.item`이 1건일 때 배열이 아니라 객체로 반환</td>
<td>양쪽 형태를 모두 수용해 파싱한다 (EI-KT-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-006</td>
<td>상세 조회(공통정보 · 소개정보)가 네트워크 오류 · 5xx로 실패</td>
<td>지수 백오프로 **2회 재시도**, 그래도 실패하면 `KTO_FETCH_FAILED`로 해당 콘텐츠만 확인 불가 처리한다 (FR-AU-027 · 028)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-007</td>
<td>실패 콘텐츠가 전체의 50% 초과</td>
<td>`AUDIT_PARTIAL`로 승격해 점수를 산출하지 않고 `부분 검수` 배지를 표시한다. 반쪽 데이터로 나온 점수가 가장 위험하다 (FR-AU-029)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-008</td>
<td>동일 콘텐츠가 조회마다 다른 값을 반환</td>
<td>검수 실행 내 캐시된 최초 응답을 기준으로 판정한다. 실행 중 값이 흔들려 결정론성이 깨지지 않게 한다 (NF-PF-011 · EX-CM-007)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-009</td>
<td>응답 지연으로 응답 타임아웃(10초) 초과</td>
<td>실패로 간주해 재시도 정책을 적용한다. 무한 대기하지 않는다 (EI-CM-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-010</td>
<td>확정된 `contentid`의 상세 조회가 **성공 응답이지만 0건**</td>
<td>`CONTENT_NOT_FOUND`로 해당 콘텐츠만 확인 불가 처리하고 나머지는 정상 판정한다. 공사 API는 존재하지 않는 ID에 대해 `resultCode 0000`  • 빈 `items`를 반환하므로(EX-EI-004) **조회 실패와 경로가 다르다.** `KTO_FETCH_FAILED`로 오분류하지 않으며, 재시도하지 않는다(재시도해도 결과가 같다). 비표출 전환(`CONTENT_HIDDEN`)과도 구분한다 — 그쪽은 차단, 이쪽은 확인 불가다</td>
<td>M</td>
</tr>
</table>
### 8-2. 지도 · 기상 · LLM
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-EI-020</td>
<td>미래 운행 정보 길찾기 호출 실패 · 출발 시각이 과거</td>
<td>자동차 길찾기로 폴백하고 화면에 "현재 시각 기준" 표기를 부착한다 (EI-KM-002)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-021</td>
<td>지도 API가 HTTP 200이지만 `result_code`가 0이 아님</td>
<td>`ROUTE_NOT_FOUND`로 해당 구간만 확인 불가 처리한다. HTTP 상태만 보고 성공으로 판단하지 않는다 (EI-KM-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-022</td>
<td>지도 API 전면 장애</td>
<td>지수 백오프로 2회 재시도(EI-CM-005 · FR-AU-028) 후에도 실패하면 R08 전체를 `ROUTE_PROVIDER_FAILED`로 확인 불가 처리하고 검수는 계속 진행한다. 구간 → 규칙으로 확대하는 명시적 예외다. **직선거리 추정으로 대체하지 않는다** (EI-KM-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-023</td>
<td>이동수단이 대중교통</td>
<td>애초에 호출하지 않고 `TRANSIT_NOT_SUPPORTED`로 확인 불가 처리한 뒤 확인 필요 목록에 등록한다. 자동차 시간으로 대체 제시하지 않는다 (FR-RU-086)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-024</td>
<td>기상 예보 API 실패</td>
<td>지수 백오프로 2회 재시도(EI-CM-005) 후 `FORECAST_UNAVAILABLE`로 기록하고 **평년 강수일수 테이블로 자동 강등**해 판정을 계속한다. 화면 표기를 "평년 기준"으로 바꾼다. R09 전체를 확인 불가로 만들지 않는다 (EI-WX-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-025</td>
<td>해당 지역 · 월의 평년 데이터가 테이블에 없음</td>
<td>`CLIMATE_DATA_MISSING`으로 R09만 확인 불가 처리한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-026</td>
<td>LLM 응답에 지시문처럼 보이는 내용이 포함 (프롬프트 주입 의심)</td>
<td>스키마 검증에서 걸러지며, 통과하지 못하면 확인 불가로 강등된다. LLM 출력이 등급 · 점수 · 권한에 직접 영향을 주는 경로가 없으므로 영향은 조각 1건으로 갇힌다 (NF-SC-012)</td>
<td>M</td>
</tr>
<tr>
<td>EX-EI-027</td>
<td>외부 API가 부분적으로 느려 검수 목표 시간(15초)을 초과</td>
<td>검수를 중단하지 않고 완료까지 진행하되 진행률을 계속 갱신한다. 응답 시간 초과 자체는 실패가 아니다 (NF-PF-015)</td>
<td>M</td>
</tr>
</table>
---
## 9. 검수 실행 (EX-AU)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-AU-001</td>
<td>미확정 관광지가 남은 채 검수 요청</td>
<td>`PLACE_UNRESOLVED`로 거부하고 미확정 항목으로 안내한다. 기획 화면의 검수 시작(`POST products/{id}/handoff`)도 같은 조건이며 남은 건수(`pendingCount`)를 함께 돌려준다. 단 사용자가 검수 시작 창에서 "이대로 검수 시작"을 골라 `excludePending`을 보내면 남은 곳을 직접 정한 곳(`EXCLUDED`)으로 두고 넘긴다 — 한 트랜잭션이며 일정에서 지우지 않는다 (FR-PL-001 · EX-PL-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-002</td>
<td>검수 작업이 30분을 초과해도 완료되지 않음</td>
<td>`AUDIT_TIMEOUT`으로 `FAILED` 확정한다. `RUNNING` 상태로 영구 정체되지 않아야 하며 사용자는 재실행할 수 있다 (NF-AV-008). 재배포 · 재시작으로 끊긴 작업은 종료 신호에 `INTERNAL_ERROR` 로 닫고, 그러지 못한 것은 만든 지 30분이 지나면 검수 요청 · 진행 조회 · 확정 · 부팅 때 닫는다 — 시각 조건 없이 닫으면 재배포 중 옛 컨테이너가 돌리는 검수를 끊는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-003</td>
<td>검수 진행 중 사용자가 화면을 이탈 후 재진입</td>
<td>작업 ID로 진행 상태를 이어서 표시한다. 새 검수를 중복 실행하지 않는다 (UI-ST-003)</td>
<td>S</td>
</tr>
<tr>
<td>EX-AU-004</td>
<td>같은 상품에 검수 요청이 중복 도착</td>
<td>진행 중인 작업이 있으면 새 작업을 만들지 않고 기존 작업 ID를 반환한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-005</td>
<td>동시 실행 검수가 3건을 초과</td>
<td>큐에 대기시키고 대기 중임을 표시한다. 거부하지 않는다 (NF-CP-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-006</td>
<td>특정 규칙 평가 중 예외 발생</td>
<td>**해당 규칙만** 확인 불가 처리하고 나머지 규칙은 정상 수행한다. 규칙 하나가 전체 검수를 죽이지 않는다. 확인 불가는 대상 항목 없이 규칙 단위로 결과에 남기고 사유코드는 `INTERNAL_ERROR` 다 — 규칙 실패 전용 코드를 두지 않는다(EX-CM-021). 남기지 않으면 그 규칙은 「문제 없음」과 같아 보인다(FR-RU-051)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-007</td>
<td>부분 검수 상태에서 점수 표시 요청</td>
<td>숫자 점수를 표시하지 않고 `부분 검수` 배지와 실패 콘텐츠 수를 표시한다 (UI-S3-006 · DR-IN-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-008</td>
<td>차단 finding이 있는 상태에서 출시 승인 요청</td>
<td>`FORBIDDEN_ACTION`으로 거부한다. 화면 버튼 비활성화만으로 충족하지 않으며 API에서도 막아야 한다 (PM-NG-002)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-009</td>
<td>차단 finding에 무시 요청</td>
<td>`FORBIDDEN_ACTION`으로 거부한다. 데이터베이스 제약으로도 강제한다 (PM-NG-001 · DR-IN-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-010</td>
<td>과거 검수 실행 결과 수정 요청</td>
<td>수정 API를 제공하지 않는다. 요청 경로 자체가 존재하지 않아야 한다 (PM-NG-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-011</td>
<td>PDF 리포트 생성 실패</td>
<td>`REPORT_FAILED`로 거부하고 재시도 수단을 제공한다. 검수 결과 자체는 영향받지 않는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-AU-012</td>
<td>finding 무시 요청에 사유가 없음 (빈 값 · "기타"를 고르고 내용을 비움)</td>
<td>`DISMISS_REASON_REQUIRED`(400)로 거부하고 finding 상태를 바꾸지 않는다. 사유는 건별 무시가 기준을 느슨하게 적용하는 유일한 길이라 필수이며, 자주 쓰는 사유 「고객 요청 사항」 · 「계약 업체 · 확정 일정」 · 「전화로 직접 확인함」 중 하나 또는 기타(직접 입력, 내용 필수)다. 화면 버튼 비활성화만으로 충족하지 않으며 API에서도 막아야 한다. 저장한 사유는 리포트에 무시 항목별로 찍힌다 (FR-AU-068 · FR-PA-064)</td>
<td>M</td>
</tr>
</table>
---
## 10. 패치와 되돌리기 (EX-PA)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-PA-001</td>
<td>선택한 수정안 사이에 충돌 검출</td>
<td>`PATCH_CONFLICT`로 확정을 차단하고 **어느 두 수정안이 충돌하는지** 지목한다. 시스템이 자동 해제하지 않는다 (FR-PA-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-002</td>
<td>확정 시점에 일정이 이미 다른 경로로 변경됨</td>
<td>`PATCH_STALE`로 거부하고 재검토를 유도한다. 오래된 스냅샷 기준으로 덮어쓰지 않는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-003</td>
<td>패치 반영 도중 실패</td>
<td>전체를 원상 복구한다. 일부만 반영된 상태를 남기지 않는다. 일정 변경은 단일 트랜잭션으로 처리한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-004</td>
<td>패치 확정 후 자동 재검수가 실패</td>
<td>일정 변경은 유지하되 검수 결과를 `FAILED`로 남기고 재실행 수단을 제공한다. 일정을 자동 롤백하지 않는다 (EX-CM 원칙 ③)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-005</td>
<td>재검수 결과 점수 하락 · 차단 증가</td>
<td>예외가 아니다. 자동 롤백하지 않고 경고 배너와 되돌리기 수단을 제시해 사용자가 결정하게 한다 (FR-PA-027)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-006</td>
<td>되돌릴 패치가 없거나 1단계를 초과한 되돌리기 요청</td>
<td>`UNDO_UNAVAILABLE`로 거부한다. 되돌리기는 직전 1단계까지만 지원한다 (FR-PA-026)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PA-007</td>
<td>수정안이 참조하는 대체 관광지가 확정 시점에 비표출로 전환</td>
<td>재검수에서 `CONTENT_HIDDEN` 차단으로 잡힌다. 확정 자체는 막지 않되 결과에서 즉시 드러나야 한다</td>
<td>M</td>
</tr>
</table>
---
## 11. 감시와 배치 (EX-MO)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-MO-001</td>
<td>배치 실행 자체가 실패 (네트워크 · 서버 재시작)</td>
<td>`last_covered`를 갱신하지 않아 다음 회차가 누락 구간을 자동 재조회한다. 별도 복구 절차를 두지 않는다 (FR-MO-014 · NF-AV-007)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-002</td>
<td>평일 배치 조회 결과가 0건</td>
<td>`BATCH_EMPTY`로 기록하고 `last_covered`를 갱신하지 않는다. 공사 동기화 지연 신호로 간주해 다음 배치가 재조회한다 (FR-MO-015)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-003</td>
<td>비표출 전환이 페이지 상한(20)을 초과 (2026-07-28 3,206건 사례)</td>
<td>`BATCH_HIDDEN_OVERFLOW`로 배치를 중단하고, 등록 상품에 포함된 `contentid`만 개별 확인하는 방식으로 전환한다. 상세 조회가 「없음」이면 표출 중단으로 알리고(재검수가 R06-b 차단), 있으면 지문을 비교한다. 예산이 떨어지거나 못 읽은 곳이 있으면 `last_covered` 를 올리지 않아 다음 회차가 그 날짜를 다시 본다 (FR-MO-016)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-004</td>
<td>배치 중단 기간이 길어 순회할 날짜가 많음</td>
<td>하루씩 순회하되 예산 소진율 80%에 도달하면 중단하고 다음 회차로 이월한다. 예산을 초과해 몰아 처리하지 않는다 (PM-SY-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-005</td>
<td>지문의 판정 필드 목록이 직전과 다름 (규칙 변경 등)</td>
<td>`FINGERPRINT_INCOMPARABLE`로 비교를 건너뛰고 전 규칙 재판정만 수행한다. **변경 알림을 만들지 않는다** — 실제 변경이 아니기 때문이다 (DR-FP-006)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-006</td>
<td>지문은 같은데 `modifiedtime`만 변경</td>
<td>판정 무관 변경으로 분류해 알림을 만들지 않고 조회 시각만 갱신한다 (DR-FP-011)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-007</td>
<td>양쪽 `normalized_json`이 모두 `NULL`인데 지문이 달라짐</td>
<td>변경은 감지하되 "내용 확인 불가 · 재확인 필요"로 알린다. 무엇이 바뀌었는지는 표시할 수 없다 (DR-FP-012)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-008</td>
<td>출발일이 지난 상품에 변경이 감지됨</td>
<td>알림을 생성하지 않는다. 감시 대상에서 제외된 상태다 (FR-MO-018)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-009</td>
<td>동일 콘텐츠의 동일 변경으로 알림이 반복 생성</td>
<td>동일 `contentid`와 동일 변경 지문 쌍은 재노출하지 않는다. 새로운 변경이 발생하면 다시 알린다 (FR-MO-036)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MO-010</td>
<td>비표출 전환 알림에 무시 요청</td>
<td>`FORBIDDEN_ACTION`으로 거부한다. 차단 등급이므로 무시할 수 없다 (FR-MO-037 · PM-NG-010)</td>
<td>M</td>
</tr>
</table>
---
## 12. 쿼터와 예산 (EX-QT)
<callout icon="🚨" color="red_bg">
	심사 기간에 예산이 소진돼 검수가 불가능해지면 **서비스가 죽은 것과 같다.** 이 절은 NF-AV-005와 짝을 이루는 방어선이다.
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-QT-001</td>
<td>일일 예산 소진율 80% 이상</td>
<td>`BUDGET_THRESHOLD` — 자동 배치를 즉시 중지한다. 사용자의 온디맨드 검수는 계속 허용한다 (FR-OP-003)</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-002</td>
<td>일일 예산 소진율 100%</td>
<td>`BUDGET_EXHAUSTED` — 신규 검수 실행을 차단하고 **재개 시점(익일 00:00 KST)**을 안내한다 (FR-OP-004 · UI-ST-007)</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-003</td>
<td>검수 실행 도중 예산이 소진됨</td>
<td>자체 예산 소진이면 진행 중인 검수는 끝까지 완료하고 다음 요청부터 차단한다. 단, **공사 응답으로 쿼터 초과가 확인되면 EX-EI-003을 우선 적용**해 즉시 중단하며, 잔여 조회 실패로 50%를 넘으면 `AUDIT_PARTIAL`로 확정한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-004</td>
<td>예산 설정값이 계정 한도를 초과하도록 변경 시도</td>
<td>거부한다. 개발계정 1,000 · 운영계정 100,000을 상한으로 강제한다 (DR-IN-008 · FR-OP-008)</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-005</td>
<td>공사가 반환한 쿼터 초과와 자체 예산 집계가 불일치</td>
<td>**공사 응답을 우선**해 즉시 차단하고, 자체 카운터를 실제 값에 맞춰 보정한다</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-006</td>
<td>예산 집계 기준일이 UTC로 계산됨</td>
<td>설계 결함이다. `quota_date`는 KST 기준으로 채워야 하며, 어긋나면 집계가 9시간 밀린다 (DR-CF-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-007</td>
<td>심사 기간 중 예산 여유 확보 필요</td>
<td>자동 배치를 중단해 온디맨드 검수용 예산을 확보한다. 배치를 중단해도 "다시 검수"로 변경 감지를 시연할 수 있어야 한다 (NF-AV-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-QT-008</td>
<td>기획 화면(자동완성 · 장소 정보 한 줄 · 장소 담기)이나 에이전트 도구가 공사를 부를 때 일일 예산 소진율 100%</td>
<td>검수와 같은 게이트에서 `BUDGET_EXHAUSTED`(429)로 **조회만** 멈춘다. 게이트는 부르려는 서비스의 예산을 본다(EI-CM-012). 화면은 이때만 조회 자리에 무엇이 · 왜 · 다음에 무엇을 할지 담은 문구를 보인다 — "오늘 쓸 수 있는 관광정보 조회를 모두 써서 장소 정보를 더 불러올 수 없어요. 내일 0시부터 다시 볼 수 있고, 일정 입력과 저장은 지금도 할 수 있어요." 일정 입력 · 자동 저장은 막지 않고 호출 수는 적지 않는다. 에이전트 실행이면 EX-AG-001 문구를 쓴다 (FR-PL-018 · EX-MS-001)</td>
<td>M</td>
</tr>
</table>
---
## 13. 기획 화면 (EX-PL)
<callout color="blue_bg">
	기획 화면은 판정하지 않으므로 이 절의 예외는 **확인 불가 등급을 만들지 않는다.** 처리 단위는 요청이다 — 조회 하나가 비거나 실패해도 그 자리만 비우고 입력과 다른 영역은 그대로 둔다. 규칙 번호 · 통과 여부 · 사유 코드를 화면에 적지 않는다 (FR-PL-021).
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-PL-001</td>
<td>장소 담기에서 고른 종류 · 필터에 맞는 후보가 0건</td>
<td>예외가 아니라 빈 상태다. 목록 자리에 빈 상태를 보이고, 필터가 켜져 있으면 "필터 모두 끄기"를 함께 둔다. 필터를 저절로 풀거나 다른 종류 · 지역의 후보로 채우지 않는다 (FR-PL-011 · UI-CM-043)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-002</td>
<td>축제 · 공연 종류에서 여행 날짜 앞뒤 3일에 등록된 행사가 0건</td>
<td>예외가 아니라 빈 상태다. "여행 날짜 앞뒤 3일에 등록된 행사가 없어요"만 보이고 출발일 옮기기 버튼은 두지 않는다 (FR-PL-014)</td>
<td>S</td>
</tr>
<tr>
<td>EX-PL-003</td>
<td>연관 관광지 정보를 장소 목록과 잇지 못함 — 응답에 `contentid`가 없고 이름 · 시군구 대조가 하나로 정해지지 않음</td>
<td>그 장소는 순위 없이 목록에 보인다. 대조 후보가 여럿이면 어느 쪽에도 순위를 붙이지 않는다 (EI-KT-024 · 설계 원칙 3)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-004</td>
<td>새 공사 서비스(무장애 여행 정보 · 반려동물 동반여행정보 · 관광지별 연관 관광지 정보 · 두루누비) 호출 실패 · 타임아웃</td>
<td>재시도(EI-CM-005) 뒤에도 실패하면 **그 서비스를 쓰는 종류 칩 · 필터 · 정렬 · 카드 칸만** "지금은 볼 수 없어요"로 두고 나머지 종류 · 목록 · 입력은 그대로 쓴다. 응답의 그 값은 `null`이며 "휠체어 가능 0곳"처럼 0으로 적지 않는다. 화면에 서비스 이름을 적지 않는다. 사유 코드는 `KTO_FETCH_FAILED` 다 (EI-CM-011 · FR-PL-018 · EX-MS-003)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-005</td>
<td>아직 고르지 않은 장소(`PENDING`)가 남은 채 "검수 시작"을 누름</td>
<td>확인 창에 남은 장소 목록과 "AI로 한 번에 찾기" · "이대로 검수 시작" · "돌아가기"를 준다. "이대로 검수 시작"을 고를 때만 남은 곳을 직접 정한 곳(`EXCLUDED`)으로 두고 검수를 요청하며, 일정에서 지우지 않는다. 창을 거치지 않은 요청은 EX-AU-001로 거부된다 (FR-PL-001)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-006</td>
<td>장소 정보 한 줄의 이동 구간에 좌표가 없음 — 앞 줄을 아직 고르지 않았거나 앞 줄 · 이 줄이 직접 정한 곳</td>
<td>그 구간의 걸리는 시간을 적지 않고 카카오모빌리티를 부르지 않는다. 기획 화면 어디에도 이동 합계를 두지 않는다 — 잰 구간만 더한 합계는 실제보다 짧은 숫자가 된다. 이동 시간이 충분한지는 검수의 R08이 판정하고, 검수에서 그 앞뒤 구간은 `COORD_MISSING`으로 확인 불가가 된다 (FR-PL-005 · EX-MC-006 · 설계 원칙 3)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-007</td>
<td>비표출(`showflag=0`) 콘텐츠</td>
<td>장소 찾기 · 장소 담기 · 기획 에이전트의 모든 목록에서 뺀다. 이미 고른 곳이 기획하는 사이 비표출로 바뀌면 기획 화면에서 막지 않고 첫 검수에서 `CONTENT_HIDDEN` 차단으로 잡힌다 (FR-PL-019 · EX-MC-005)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-008</td>
<td>넣을 위치 앞 장소를 아직 고르지 않았거나 그 장소가 직접 정한 곳 (3km 기준점 없음)</td>
<td>식당 · 카페 · 숙소 칩을 끄고 "장소를 고른 뒤"로 적는다. 조회를 부르지 않으며(`ANCHOR_REQUIRED`) 다른 기준점으로 대신 세지 않는다 (FR-PL-010)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-009</td>
<td>식당 · 카페 · 숙소 칩의 3km 안 결과가 0건</td>
<td>예외가 아니라 빈 상태다. 칩 숫자를 0으로 적고 목록을 비워 둔다. 반경을 넓히거나 시군구 전체 목록으로 대신하지 않는다 (FR-PL-010 · 011)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-010</td>
<td>장소 찾기 결과가 0곳 (장소 칸 자동완성 · 기획 에이전트 카드의 "장소 찾기")</td>
<td>숫자 대신 "관광정보에 올라 있는 이름으로 검색해 보세요"를 보이고, 목록 맨 아래에 "찾는 곳이 없나요? 직접 정한 곳으로 두기"를 둔다. 기획 에이전트가 장소 이름이 없다고 본 줄은 그 문구로 검색하지 않고 "숙소 이름을 적어 보세요"(식사 줄이면 식당)를 보인다 (FR-PL-004 · FR-AG-011 · UI-CM-043)</td>
<td>M</td>
</tr>
<tr>
<td>EX-PL-011</td>
<td>일정에 넣은 걷기 길의 이름을 찾지 못함 (두루누비 장애 · 목록에서 사라짐)</td>
<td>그 줄의 장소 칸에 "걷기 길"로 보이고 일정에서 지우지 않는다. 코스 이름을 짐작하거나 다른 값으로 채우지 않는다. 사용자가 장소 칸에 이름을 쓰면 그 글을 보인다 (DR-MD-005 · EI-KT-025)</td>
<td>S</td>
</tr>
</table>
---
## 14. AI 에이전트 (EX-AG)
<callout color="blue_bg">
	에이전트는 제안만 하므로 실패해도 **검수 결과 · 점수 · 일정은 바뀌지 않는다.** 처리 단위는 요청이며, 사람이 누르는 기존 버튼은 어떤 경우에도 그대로 동작한다 (FR-AG-001 · 005).
</callout>
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-AG-001</td>
<td>에이전트 실행 중 LLM 호출 실패 · 시간 초과(30초) · 예산 소진</td>
<td>실행 전에 예산이 막히면 `BUDGET_EXHAUSTED`(429)로 거절한다. 실행한 뒤의 실패 · 시간 초과 · 도구 호출의 예산 소진은 거절하지 않고 200 응답에 끝난 항목만 싣고 `incomplete`(사유 `LLM_UNAVAILABLE` · `BUDGET_EXHAUSTED`)를 채운다. 화면은 어느 경우든 "지금은 AI로 정리할 수 없어요"를 보이고, 사람이 하는 길(장소 찾기 · 직접 정한 곳으로 두기 · 확인했어요 · 다시 검수 · 이 지역으로 새 상품 기획)은 그대로 둔다 (FR-AG-005 · EI-LM-010 · API 4-11)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AG-002</td>
<td>에이전트가 일부 항목만 처리하고 끝남 (시간 상한에 닿거나 중간에 실패)</td>
<td>끝난 항목만 결과로 보이고, 끝나지 않은 줄 · 곳에는 결과 카드를 붙이지 않은 채 사람이 하는 길을 그대로 둔다. 끝나지 않은 항목을 "찾지 못했어요"나 "등록된 전화번호가 없어요"로 적지 않는다 (FR-AG-005 · 설계 원칙 3)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AG-003</td>
<td>에이전트 출력에 그 실행의 도구 결과에 없던 값 (`contentid` · finding id · 알림 id · 상품 id · 전화번호)</td>
<td>그 항목만 버리고 나머지 결과는 보인다. 전화번호는 `null`로 바꿔 EX-AG-005 문구로 보인다. 로그에는 버린 건수만 남기고 값 · 문장은 남기지 않는다 (FR-AG-003 · EI-LM-008)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AG-004</td>
<td>같은 계정에서 같은 에이전트를 실행하는 중에 다시 요청</td>
<td>앞 실행이 끝날 때까지 그 버튼을 비활성한다. 화면 비활성화만으로 충족하지 않으며 서버도 같은 계정의 같은 에이전트를 동시에 하나만 돌린다 — 도는 중에 온 요청은 실행하지 않고 `RATE_LIMIT_EXCEEDED`(429)로 거절하며, 화면은 "이미 정리하고 있어요. 끝나면 다시 눌러 주세요"를 보인다 (FR-AG-002 · EI-LM-007)</td>
<td>M</td>
</tr>
<tr>
<td>EX-AG-005</td>
<td>검수 에이전트가 정리한 곳에 전화번호가 없음 (공통정보 `tel` · 소개정보 `infocenter` 계열이 모두 비었거나 도구 결과 밖 번호를 버림)</td>
<td>전화번호 자리에 "등록된 전화번호가 없어요. 지역 관광안내소에 물어보세요"라고만 적는다. 번호를 짐작하거나 다른 곳의 번호로 채우지 않는다 (FR-AG-021 · EX-PS-010)</td>
<td>M</td>
</tr>
</table>
---
## 15. 시스템 · 인증 · 권한 (EX-SY)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>상황</td>
<td>처리</td>
<td>등급</td>
</tr>
<tr>
<td>EX-SY-001</td>
<td>미인증 상태로 API 호출</td>
<td>`NOT_AUTHENTICATED`로 거부한다. 화면 리다이렉트만으로 충족하지 않으며 API 레벨에서 막아야 한다 (PM-AC-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-002</td>
<td>세션 만료 후 요청</td>
<td>`NOT_AUTHENTICATED`로 거부하고 재로그인으로 유도한다. 작성 중이던 입력은 화면에 유지해 유실을 막는다</td>
<td>S</td>
</tr>
<tr>
<td>EX-SY-003</td>
<td>타 계정의 리소스 접근 시도</td>
<td>`NOT_FOUND`로 응답해 **존재 여부를 노출하지 않는다.** 403은 그 ID의 리소스가 있다는 정보를 흘린다. 근거인 PM-DA-004는 S로 정의되어 있으나, 18장 전수 테스트와 비기능 수용 기준이 이를 필수로 요구하므로 **본 문서에서 M으로 상향 확정한다**</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-004</td>
<td>인증 실패 (아이디 없음 · 비밀번호 불일치)</td>
<td>둘을 구분하지 않고 동일 메시지로 응답한다 (UI-S0-003)</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-005</td>
<td>전면 금지 동작(PM-NG) 시도</td>
<td>`FORBIDDEN_ACTION`으로 거부하고 시도 사실을 로그에 남긴다. 어떤 주체도 예외가 아니다</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-006</td>
<td>데이터베이스 연결 실패</td>
<td>`/health`가 실패 상태를 반환하고, 사용자에게는 일시적 오류로 안내한다. 인증 정보나 연결 문자열을 노출하지 않는다 (NF-AV-004)</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-007</td>
<td>동일 이메일로 중복 회원가입</td>
<td>거부한다. 이미 가입된 계정인지를 상세히 알리지 않는다</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-008</td>
<td>검수 실행 호출 빈도가 분당 5회 초과</td>
<td>`RATE_LIMIT_EXCEEDED`(429)로 거부하고 `Retry-After` 뒤에 다시 시도하도록 안내한다. 다시 검수와 검수 시작을 한 창으로 세고, 리포트 생성 · 에이전트 호출의 분당 상한(API 3-4)도 같다. 공개된 테스트 계정은 검수 실행 · 리포트 생성 상한을 세지 않는다. 기획 조회는 분당 상한 없이 예산 게이트만 둔다 (NF-SC-010)</td>
<td>S</td>
</tr>
<tr>
<td>EX-SY-009</td>
<td>인스턴스 콜드 스타트로 첫 요청이 지연</td>
<td>오류로 처리하지 않는다. 10초 이내 응답을 목표로 하며 헬스체크로 예방한다 (NF-AV-002 · 003)</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-010</td>
<td>분류되지 않은 서버 예외</td>
<td>`INTERNAL_ERROR`로 응답하고 서버 로그에 전문을 남긴다. 개발 기간 중 이 코드가 나오면 전용 코드로 분류해야 한다 (EX-CM-021)</td>
<td>M</td>
</tr>
<tr>
<td>EX-SY-011</td>
<td>회사 기준을 표준보다 느슨하게 저장하려 함 (연속 일정 기준 6시간 초과 · 최소 식사 시간 60분 미만)</td>
<td>`SETTING_NOT_STRICTER`(400)로 거부하고 저장된 값과 변경 이력을 바꾸지 않는다. 화면은 그 칸 아래에 "저장하지 않았어요. 회사 기준은 TourLint 표준보다 엄격하게만 정할 수 있어요(연속 일정 6시간 이하 · 식사 60분 이상). 특정 상품만 기준을 풀려면 그 상품의 검수 결과에서 사유를 달아 무시해 주세요."를 보인다. 회사 전체를 느슨하게 하는 다른 경로를 두지 않는다 (FR-OP-022 · FR-RU-072)</td>
<td>M</td>
</tr>
</table>
---
## 16. 사용자 메시지 규칙 (EX-MS)
<table fit-page-width="true" header-row="true">
<tr>
<td>ID</td>
<td>요구사항</td>
<td>등급</td>
</tr>
<tr>
<td>EX-MS-001</td>
<td>모든 예외 메시지는 **무엇이 · 왜 · 다음에 무엇을** 세 요소를 담아야 한다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-MS-002</td>
<td>사유 코드를 화면에 그대로 노출하지 않는다. 문의가 필요한 경우에만 참조용으로 작게 표기할 수 있다.</td>
<td>M</td>
</tr>
<tr>
<td>EX-MS-003</td>
<td>외부 서비스 장애 메시지에는 제공자를 특정하지 않고 "일시적으로 조회할 수 없습니다" 형태로 안내한다. 다만 판정 근거 영역에는 외부 참고 출처를 정상 표기한다.</td>
<td>S</td>
</tr>
<tr>
<td>EX-MS-004</td>
<td>확인 불가는 **실패가 아니라 상태**로 표현해야 한다. "오류", "실패" 같은 단어를 쓰지 않고 "데이터에서 확인할 수 없습니다"로 안내한다 (TM-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MS-005</td>
<td>메시지에 "정상", "이상 없음", "문제 없음"을 데이터 부재 상황에서 사용하지 않는다 (TM-009)</td>
<td>M</td>
</tr>
<tr>
<td>EX-MS-006</td>
<td>파괴적 동작(상품 삭제 · 되돌리기) 전에는 확인 단계를 두어야 한다 (UI-ST-012)</td>
<td>M</td>
</tr>
</table>
**메시지 예시**
<table fit-page-width="true" header-row="true">
<tr>
<td>사유 코드</td>
<td>화면 문구</td>
</tr>
<tr>
<td>`KTO_FETCH_FAILED`</td>
<td>일시적으로 조회할 수 없어 이 관광지는 확인 불가로 처리했습니다. 잠시 후 「다시 검수」로 다시 시도해 보세요.</td>
</tr>
<tr>
<td>`PARSE_TARGET_VARIES`</td>
<td>운영시간이 점포별로 다르게 안내되어 데이터에서 확인할 수 없습니다. 출시 전 운영기관에 직접 확인해 주세요.</td>
</tr>
<tr>
<td>`TRANSIT_NOT_SUPPORTED`</td>
<td>대중교통 이동시간은 데이터로 확인할 수 없습니다. 구간별 소요시간을 직접 확인해 주세요.</td>
</tr>
<tr>
<td>`AUDIT_PARTIAL`</td>
<td>대상 8곳 중 5곳을 조회하지 못해 점수를 산출하지 않았습니다. 잠시 후 다시 검수해 주세요.</td>
</tr>
<tr>
<td>`PATCH_CONFLICT`</td>
<td>선택한 수정안 중 2건이 같은 일정을 서로 다르게 바꿉니다. 둘 중 하나를 해제한 뒤 확정해 주세요.</td>
</tr>
<tr>
<td>`BUDGET_EXHAUSTED`</td>
<td>오늘 사용할 수 있는 조회량을 모두 사용했습니다. 내일 0시 이후 다시 검수할 수 있습니다.</td>
</tr>
<tr>
<td>`RATE_LIMIT_EXCEEDED`</td>
<td>요청이 몰려 잠시 멈췄습니다. 잠시 후 다시 시도해 주세요. 같은 AI 정리가 진행 중이면 "이미 정리하고 있어요. 끝나면 다시 눌러 주세요"를 보입니다.</td>
</tr>
<tr>
<td>`CONTENT_HIDDEN`</td>
<td>이 관광지는 표출이 중단되어 정보를 표시할 수 없습니다. 일정은 유지되며 대체 관광지를 제안합니다.</td>
</tr>
</table>
---
## 17. 복구 정책 요약
<table fit-page-width="true" header-row="true">
<tr>
<td>대상</td>
<td>자동 재시도</td>
<td>실패 확정 시</td>
<td>사용자 복구 수단</td>
</tr>
<tr>
<td>공사 상세 조회</td>
<td>지수 백오프 2회</td>
<td>콘텐츠 단위 확인 불가</td>
<td>다시 검수</td>
</tr>
<tr>
<td>LLM 정규화</td>
<td>1회</td>
<td>조각 단위 확인 불가</td>
<td>다시 검수</td>
</tr>
<tr>
<td>지도 API</td>
<td>지수 백오프 2회</td>
<td>구간 단위 확인 불가</td>
<td>다시 검수</td>
</tr>
<tr>
<td>기상 예보 API</td>
<td>지수 백오프 2회</td>
<td>평년 테이블로 강등 (확인 불가 아님)</td>
<td>–</td>
</tr>
<tr>
<td>검수 작업</td>
<td>없음</td>
<td>30분 초과 시 FAILED</td>
<td>재실행</td>
</tr>
<tr>
<td>패치 확정</td>
<td>없음 (상태 변경)</td>
<td>전체 원상 복구</td>
<td>재선택 후 재확정</td>
</tr>
<tr>
<td>자동 배치</td>
<td>없음</td>
<td>`last_covered` 미갱신</td>
<td>다음 회차 자동 복구</td>
</tr>
<tr>
<td>PDF 생성</td>
<td>없음</td>
<td>거부</td>
<td>재요청</td>
</tr>
<tr>
<td>새 공사 서비스 조회 (기획 화면)</td>
<td>지수 백오프 2회</td>
<td>그 종류 · 필터만 "지금은 볼 수 없어요" (확인 불가 아님)</td>
<td>칩 · 필터를 다시 누름</td>
</tr>
<tr>
<td>기획 조회 · 에이전트 예산 소진</td>
<td>없음</td>
<td>조회만 중지 · 입력과 저장은 계속</td>
<td>익일 00:00 KST 이후 다시 조회</td>
</tr>
<tr>
<td>검수 시작 (고르지 않은 장소 남음)</td>
<td>없음 (상태 변경)</td>
<td>422 `PLACE_UNRESOLVED` · 일정 불변</td>
<td>장소 고르기 또는 "이대로 검수 시작"</td>
</tr>
<tr>
<td>에이전트 실행</td>
<td>실행 전체는 없음 (사람이 누를 때만 · 30초 상한)</td>
<td>200 · 끝난 항목만 + `incomplete`(`LLM_UNAVAILABLE` · `BUDGET_EXHAUSTED`)</td>
<td>사람이 하는 길(기존 버튼) · 다시 누르기</td>
</tr>
<tr>
<td>호출 빈도 상한 · 같은 에이전트 동시 실행</td>
<td>없음</td>
<td>429 `RATE_LIMIT_EXCEEDED` · 상태 불변</td>
<td>`Retry-After` 뒤 · 앞 실행이 끝난 뒤 다시 누르기</td>
</tr>
<tr>
<td>회사 기준 저장 · finding 무시</td>
<td>없음 (상태 변경)</td>
<td>거부 · 값 불변 (`SETTING_NOT_STRICTER` · `DISMISS_REASON_REQUIRED`)</td>
<td>값 · 사유를 고쳐 다시 요청</td>
</tr>
</table>
---
## 18. 예외 시나리오 테스트 목록 (W7 전수 확인)
- [ ] 공사 인증키를 잘못된 값으로 바꾸면 `KTO_AUTH_ERROR`로 즉시 중단되고 재시도가 발생하지 않는다
- [ ] 응답을 XML 오류로 강제하면 JSON 파싱 오류가 아니라 인증 · 쿼터 오류로 분류된다
- [ ] 8곳 중 2곳 조회를 강제 실패시키면 6곳이 정상 판정되고 2곳만 확인 불가로 표시된다
- [ ] 8곳 중 5곳 실패 시 점수가 표시되지 않고 `부분 검수` 배지가 나타난다
- [ ] LLM 엔드포인트를 차단해도 검수가 끝까지 완료된다
- [ ] 지도 API를 차단해도 R08만 확인 불가가 되고 나머지 규칙은 판정된다
- [ ] 기상 예보 API를 차단하면 평년 기준 판정으로 강등되고 화면 표기가 바뀐다
- [ ] 이동수단을 대중교통으로 바꾸면 R08이 호출 없이 확인 불가로 처리된다
- [ ] 차단 finding에 무시 API를 직접 호출하면 거부된다
- [ ] 차단이 있는 상태에서 출시 승인 API를 직접 호출하면 거부된다
- [ ] 타 계정 리소스 ID로 조회하면 404 계열이 반환되고 존재 여부가 드러나지 않는다
- [ ] 충돌하는 수정안 2건을 선택하면 확정이 막히고 충돌 쌍이 표시된다
- [ ] 되돌리기를 두 번 연속 요청하면 두 번째가 거부된다
- [ ] 예산을 10건으로 낮추고 8건 소진 시 배치가 자동 중지된다
- [ ] 배치를 3일 중단 후 재개하면 누락된 3일치가 순회 조회된다
- [ ] 평일 배치 결과가 0건이면 `last_covered`가 갱신되지 않는다
- [ ] 비표출 전환된 콘텐츠의 명칭 · 주소 · 이미지가 화면 · API · PDF 어디에도 나타나지 않는다
- [ ] 검수 진행 중 화면을 이탈했다 재진입하면 진행률이 이어진다
- [ ] 잘못된 양식 파일을 올리면 오류 행 번호가 표시되고 서비스가 중단되지 않는다
- [ ] 골든 케이스 3건이 스키마 · 파서 변경 후에도 동일 결과를 낸다
- [ ] 회사 기준에 연속 일정 7시간이나 식사 30분을 저장하면 `SETTING_NOT_STRICTER`(400)로 거부되고 저장값과 변경 이력이 그대로다
- [ ] 사유 없이 무시 API를 호출하면 `DISMISS_REASON_REQUIRED`(400)로 거부되고 finding이 무시되지 않는다
- [ ] 고르지 않은 장소가 남은 상품에 `excludePending` 없이 검수 시작을 호출하면 422 `PLACE_UNRESOLVED`와 남은 건수가 오고, `excludePending: true`면 남은 곳이 `EXCLUDED`로 바뀌며 일정에서 사라지지 않는다
- [ ] 예산을 100%로 만들면 장소 찾기 · 장소 정보 한 줄 · 장소 담기 · 에이전트 조회가 `BUDGET_EXHAUSTED`로 멈추고 일정 입력과 자동 저장은 계속된다
- [ ] 무장애 여행 정보 픽스처를 실패로 바꾸면 휠체어 가능 필터만 "지금은 볼 수 없어요"가 되고 다른 종류 · 목록은 그대로다. 건수가 0으로 표시되지 않는다
- [ ] 앞 줄이 직접 정한 곳이면 장소 정보 한 줄에 걸리는 시간이 없고 카카오모빌리티 호출이 일어나지 않으며, 기획 화면 어디에도 이동 합계가 없다
- [ ] 넣을 위치 앞 장소를 고르지 않으면 식당 · 카페 · 숙소 칩이 꺼지고 조회가 일어나지 않는다
- [ ] 비표출 콘텐츠가 장소 찾기 · 장소 담기 · 기획 에이전트 목록 어디에도 나오지 않는다
- [ ] 연관 관광지 응답과 이름 · 시군구 대조가 둘 이상 맞으면 그 장소에 순위가 붙지 않는다
- [ ] 걷기 길을 일정에 넣으면 `place_label` 은 비고 `walk_id` 만 저장되며 DB 어디에도 코스 이름이 없다. 두루누비 픽스처를 실패로 바꾸면 그 줄이 "걷기 길"로 보이고 일정에서 사라지지 않는다
- [ ] 고정 LLM 응답에 도구 결과에 없던 `contentid` · 전화번호를 넣으면 그 항목이 버려지고 로그에는 버린 건수만 남는다
- [ ] LLM을 차단하거나 30초를 넘기면 "지금은 AI로 정리할 수 없어요"가 뜨고 장소 찾기 · 직접 정한 곳으로 두기 · 확인했어요가 그대로 동작한다. 응답은 200 이고 `incomplete.reasonCode` 가 `LLM_UNAVAILABLE` 이다
- [ ] 같은 계정에서 같은 에이전트를 연달아 두 번 요청하면 두 번째 요청이 `RATE_LIMIT_EXCEEDED`(429)로 거절되고 실행이 시작되지 않는다
---
## 19. 요구사항 추적
<table fit-page-width="true" header-row="true">
<tr>
<td>영역</td>
<td>예외 ID</td>
<td>연결 요구사항</td>
</tr>
<tr>
<td>공통 원칙 · 사유 코드</td>
<td>EX-CM-001 – 022</td>
<td>FR-AU-029, NF-US-006, NF-SC-009, NF-OB-005 · 006, NF-MT-001, G-015</td>
</tr>
<tr>
<td>입력 · 업로드</td>
<td>EX-IN-001 – 012</td>
<td>FR-IN-011 · 013 · 015 · 016, NF-CP-003 · 005 · 006 · 010, NF-SC-006</td>
</tr>
<tr>
<td>관광지 매칭</td>
<td>EX-MC-001 – 007</td>
<td>FR-IN-023 – 026, FR-RU-065 – 067, UI-S2-023</td>
</tr>
<tr>
<td>해석기</td>
<td>EX-PS-001 – 010</td>
<td>FR-AU-009 · 010 · 014, FR-AU-082, DR-NM-011 · 013 · 034, DR-IN-009, DR-FP-004</td>
</tr>
<tr>
<td>외부 연동</td>
<td>EX-EI-001 – 027</td>
<td>EI-CM-004 · 005, EI-KT-002 · 004 · 005, EI-KM-002 · 005 · 009, EI-WX-006, FR-AU-027 – 029, FR-RU-086, NF-SC-012, NF-PF-011 · 015</td>
</tr>
<tr>
<td>검수 실행</td>
<td>EX-AU-001 – 012</td>
<td>FR-AU-029 · 068, FR-PA-064, FR-PL-001, PM-NG-001 · 002 · 004, DR-IN-005 · 006, NF-AV-008, NF-CP-004, UI-S3-006, UI-ST-003</td>
</tr>
<tr>
<td>패치</td>
<td>EX-PA-001 – 007</td>
<td>FR-PA-006 · 026 · 027, PM-NG-011 · 012</td>
</tr>
<tr>
<td>감시 · 배치</td>
<td>EX-MO-001 – 010</td>
<td>FR-MO-014 – 016 · 018 · 036 · 037, DR-FP-006 · 011 · 012, PM-NG-010, PM-SY-005, NF-AV-007</td>
</tr>
<tr>
<td>쿼터 · 예산</td>
<td>EX-QT-001 – 008</td>
<td>FR-OP-003 · 004 · 008, FR-PL-018, EI-CM-012, UI-ST-007, DR-CF-004, DR-IN-008, NF-AV-005</td>
</tr>
<tr>
<td>기획 화면</td>
<td>EX-PL-001 – 011</td>
<td>FR-PL-001 · 004 · 005 · 010 · 011 · 014 · 018 · 019 · 021, FR-AG-011, EI-CM-005 · 011, EI-KT-024, EX-MC-005 · 006, UI-CM-043</td>
</tr>
<tr>
<td>AI 에이전트</td>
<td>EX-AG-001 – 005</td>
<td>FR-AG-001 – 003 · 005 · 021, EI-LM-007 – 010, EX-PS-010</td>
</tr>
<tr>
<td>시스템 · 권한</td>
<td>EX-SY-001 – 011</td>
<td>PM-AC-004, PM-DA-004, PM-NG 전건, NF-AV-002 – 004, NF-SC-010, UI-S0-003, FR-OP-022, FR-RU-072</td>
</tr>
<tr>
<td>메시지 규칙</td>
<td>EX-MS-001 – 006</td>
<td>TM-009, NF-US-006, UI-ST-012</td>
</tr>
</table>
---
<callout icon="📌" color="gray_bg">
	**개정 이력**
	v1.0 (2026.08.14) — 최초 작성. 예외처리 요구사항 108건(EX-CM 12 · EX-IN 11 · EX-MC 7 · EX-PS 10 · EX-EI 17 · EX-AU 11 · EX-PA 7 · EX-MO 10 · EX-QT 7 · EX-SY 10 · EX-MS 6 — EX-CM은 001–008 · 010–011 · 020–021로 절 구분을 위해 번호를 띄어 사용, EX-EI는 001–009 · 020–027). 예외 사유 코드 38종, 예외 3원칙, 8단계 격리 분류, 복구 정책 요약 8행, W7 전수 테스트 시나리오 20건 정의. 선행 8개 문서의 실패 모드를 합집합으로 수렴.
	v1.1 (2026.08.19) — 문서 정합성 점검 반영(배치 1). EX-CM-022 신설 및 「규칙 판정 사유코드 목록」 15종 추가 — R02–R04 · R07–R10 판정 코드 명명, R01 조건부 휴무 `REST_DAY_UNCERTAIN` 신설, F07 자동 생성 항목 `PRE_DEPARTURE_CHECK` 정식 등록(API 5-7 기사용분 소급 정의).
	v1.2 (2026.08.19) — 정합성 점검 반영: 3장 격리도 도해에 '구간(segment)' 단위 추가(EX-CM-001의 8종과 일치), `ROUTE_PROVIDER_FAILED` 단위를 구간 → 규칙으로 정정(EX-EI-022의 규칙 승격과 일치). 상품 범위 확대(당일\~2박 3일, 팀 합의 2026.08.19) 반영: EX-IN-004 일정 항목 상한 30 → 45건(NF-CP-003 v1.1 정합). 예외 사유코드 38종 · 판정 사유코드 15종 체계는 변경 없음.
	v1.3 (2026.08.20) — 테스트 픽스처 설계 중 발견 반영. 예외 사유코드 `CONTENT_NOT_FOUND` 신설(콘텐츠 단위 · 확인 불가)과 EX-EI-010 추가. 공사 API는 존재하지 않는 `contentid`에 오류가 아니라 `resultCode 0000` + 빈 `items`를 반환하므로(EX-EI-004), 이 경우가 `KTO_FETCH_FAILED`에도 `CONTENT_HIDDEN`에도 해당하지 않아 판정 경로가 비어 있었다. 예외 사유코드 38종 → 39종 · EX-EI 17건 → 18건.
	v1.4 (2026.08.26) — `RAIN_RISK` 사유 서술에서 「우천 대체 없음」을 뺐다. FR-RU-093 v2.0 개정에 맞춘 것이며 사유코드 목록(15종)과 예외 사유코드(39종)는 변경 없다.
	v1.5 (2026.09.15) — 설정 탭 개편안과 기획 · 검수 · 레이더 개편안 v7.1 반영. ① **예외 사유코드 +2** — `SETTING_NOT_STRICTER`(회사 기준이 표준보다 느슨함, EX-SY-011) · `DISMISS_REASON_REQUIRED`(무시 사유 없음, EX-AU-012). 회사 기준은 엄격하게만 저장되고 기준을 느슨하게 적용하는 길은 사유를 남기는 건별 무시 하나뿐이라 두 경로 모두 API 에서 거부한다. 기획 조회의 예산 소진은 새 코드 없이 `BUDGET_EXHAUSTED` 에 「일정 입력 · 저장은 계속된다」를 더했고(EX-QT-008), 에이전트의 실패 · 시간 초과는 `LLM_UNAVAILABLE` 을 요청 단위로도 쓴다 ② EX-AU-001 에 검수 시작(handoff)도 같은 조건이며 「이대로 검수 시작」(`excludePending`)이면 남은 곳을 직접 정한 곳으로 두고 넘긴다고 병기 ③ **13절 기획 화면(EX-PL-001 – 010) · 14절 AI 에이전트(EX-AG-001 – 005) 신설** — 후보 0건 · 행사 0건 · 연관 관광지 대조 실패 · 새 서비스 장애 · 검수 시작 창 · 좌표 없는 구간(걸리는 시간을 적지 않고 합계를 두지 않음) · 비표출 · 3km 기준점 없음 · 3km 0건 · 장소 찾기 0곳, 에이전트의 실패 · 일부 완료 · 도구 결과 밖 값 · 동시 실행 · 전화번호 없음. 옛 13 – 17절은 15 – 19절로 밀렸고 EX-SY-003 의 「16장 전수 테스트」를 18장으로 고쳤다 ④ 기능 요구사항 v2.3 의 FR-IN-025 에 맞춰 EX-MC-001 을 고쳤다 — 검색 결과 0건인 줄을 자동으로 검수 제외하지 않고 아직 고르지 않은 장소로 남기며, 사용자가 고를 때만 직접 정한 곳이 된다. EX-MC-002 도 검수 진입점 비활성 대신 검수 시작 창(UI-S2-023)으로 바꿨다 ⑤ 17절 복구 정책 8행 → 13행, 18절 테스트 시나리오 20건 → 32건, 19절 추적표에 두 영역 추가. 예외처리 요구사항 110건 → 128건, 예외 사유코드 39종 → 41종(판정 사유코드 15종은 변경 없음). 연쇄 개정: API 설계 9-1(사유 코드 건수 · HTTP 매핑) · 용어 정의(사유 코드 건수) · `packages/shared` `EXCEPTION_REASON_CODE` · 기능(FR-PL · FR-AG · FR-IN-025 · FR-AU-068 · FR-OP-022) · 화면(UI-S2 · UI-CM-043) · 외부 연동(EI-CM-011 · 012 · EI-LM-007 – 010). 통합 점검 — 화면 말(EX-QT-007 · 메시지 예시 · 복구 수단 "다시 검수", 3장 도해 "직접 정한 곳")과 EX-PL-004 사유 코드(`KTO_FETCH_FAILED`)를 맞췄다. 결정 반영(2026.09.15) — ① 예외 사유코드 `RATE_LIMIT_EXCEEDED` 신설(42종) — 호출 빈도 상한(EX-SY-008)과 같은 에이전트 동시 실행(EX-AG-004)을 429 로 거절한다. 에이전트의 실행 뒤 실패 · 시간 초과 · 예산 소진은 거절이 아니라 200 `incomplete` 로 끝난 항목만 돌려준다(EX-AG-001 · 4장 · 16절 메시지 예시 · 17절 · 18절). ② 걷기 길 이름을 못 찾을 때의 EX-PL-011 신설 — 코스 이름을 저장하지 않으므로(DR-MD-005) "걷기 길"로 보인다. 예외처리 요구사항 129건, 복구 정책 14행, 테스트 시나리오 33건. EX-PL-011 원인에서 「상품 지역이 바뀜」을 뺐다 — 걷기 길 이름을 두루누비 전국 목록에서 찾으므로 지역이 바뀌어도 찾는다(2026.09.15 실호출).
	v1.8 (2026.09.25) — #772 · #774: EX-AU-002 에 재배포로 끊긴 작업을 닫는 방법(종료 신호 · 30분 기준 정리)을, EX-AU-006 에 실패한 규칙을 남기는 모양(규칙 단위 확인 불가 · `INTERNAL_ERROR`)을 적었다. 둘 다 구현이 요구를 못 지키고 있었다 — 끊긴 작업이 `RUNNING` 으로 남아 그 상품의 재검수 · 확정을 막았고, 깨진 규칙은 결과에서 조용히 빠졌다. 사유코드 43종은 그대로다.
	v1.7 (2026.09.20) — #673: EX-IN-012 신설. 등록 화면이 거절 이유를 버리고 「저장에 실패했습니다. 잠시 후 다시 시도해 주세요」만 띄웠다. 서버는 일차와 줄 번호까지 말해 주고 있었고, 막힌 원인은 대개 「+ 항목 추가」로 만들어 두고 채우지 않은 줄이었다. 이유를 그대로 보이는 것과 보내기 전에 화면이 먼저 보는 것을 규정한다.
	v1.6 (2026.09.20) — #612: 예외 사유코드 `INPUT_INVALID`(요청 · 입력 형식 오류 · 거부) 신설 — 42종 → 43종. EX-CM-021 에 따라 `INTERNAL_ERROR` 로 기록되던 원인을 분류했다. 사유코드 없이 던지던 입력 오류(허용 값 밖 · 빠진 필수 값)와 깨진 JSON 본문이 400 인데 「분류되지 않은 서버 오류」로 나가고 있었다. 입력 오류에 `NOT_FOUND` · `INTERNAL_ERROR` 를 붙이던 네 곳(검색어 없음 · contentid 없음 · 장소 담기 종류 없음)도 옮겼다. 연쇄 개정: API 설계 3-3 · 9-1 · `packages/shared` `EXCEPTION_REASON_CODE`.
</callout>
