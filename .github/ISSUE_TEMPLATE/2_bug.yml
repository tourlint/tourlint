name: 결함
description: 동작이 기대와 다르다
labels: ["bug"]
body:
  - type: textarea
    id: actual
    attributes:
      label: 증상
      description: 재현되는 픽스처(TP-01~04)가 있으면 같이.
    validations: { required: true }
  - type: textarea
    id: expected
    attributes:
      label: 기대한 동작
      description: 근거 조항이나 기대값 표 항목.
    validations: { required: true }
