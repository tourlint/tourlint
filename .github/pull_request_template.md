<!--
브랜치 흐름: 작업 브랜치 → dev → main
main 은 Railway 가 자동 배포합니다. dev 를 거치지 않고 main 으로 직접 올리지 않습니다.

길이는 1,500자 안쪽으로. 리뷰어가 한 화면에서 읽고 판단할 수 있어야 합니다.
표는 비교할 게 둘 이상일 때만, 이모지는 쓰지 않습니다.
설계 배경이 길어지면 PR 이 아니라 docs/ 에 두고 링크하세요.
-->

## 해결하는 이슈

<!--
이 PR 이 실제로 끝내는 이슈만 적습니다.
  Closes #12

dev 로 머지할 때는 GitHub 이 자동으로 안 닫아줍니다. 키워드 자동 닫기는 기본 브랜치(main)
머지에서만 동작해요. dev 머지 직후 손으로 닫습니다:
  gh issue close <n> --comment "완료 판정을 어떻게 채웠는지"

작업하다 새로 만든 후속 이슈는 여기 적지 않습니다. 해결된 게 아니라 파생된 거라서요.
그건 맨 아래 "남긴 것"에 링크만 겁니다.
-->

해당 없음

## 무엇을, 왜

<!-- 한두 문단. "뭘 바꿨다"보다 "왜 이게 필요했다"를 먼저. -->

## 검증

<!-- 안 한 건 안 했다고 적습니다. 지우면 안 본 건지 없는 건지 구분이 안 돼요. -->

- [ ] `pnpm verify` 통과 (build, lint, typecheck, test)
- [ ] 회귀 체크리스트 — `docs/기대값표.md` §8
- [ ] 파서 커버리지 — 휴무 216/239, 운영 240/266 이상

## 설계 제약

<!-- 해당 없으면 "해당 없음"으로 남깁니다. -->

- [ ] 무저장 — 공사 원문을 DB·로그에 남기지 않음
- [ ] 결정론성 — 같은 입력에 같은 결과. 규칙에서 `Date`·난수 안 씀
- [ ] 어댑터 격리 — `engine/rules`가 `external`·`persistence`를 참조하지 않음
- [ ] 인증키 — 커밋·로그·오류 메시지에 없음

## 근거 조항

<!-- FR-AU-004, DR-TD-003 처럼. 없으면 "해당 없음" -->

## 노션 문서

<!-- 계약을 바꿨으면 어느 문서 어느 항목인지. 안 바꿨으면 "개정 없음" -->

개정 없음

## 남긴 것

<!-- 일부러 안 한 것과 그 이유. 새로 만든 후속 이슈 링크. -->
