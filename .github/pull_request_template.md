<!--
제목: <타입>(<영역>): 무엇을 했는가 (명세 ID)
       feat(engine): R05 데이터 검증 불가 (FR-RU-051)
       fix(external): 연결/응답 타임아웃 분리 (EI-CM-004)

base 는 dev. main 으로 직접 올리지 않는다.
머지 조건: CI 통과 (build · lint · typecheck · test). 그 결과를 본문에 옮겨 적지 않는다.

아래는 참고용이다. 작은 변경은 한두 줄로 끝내라.
섹션을 다 채우려고 늘려 쓰지 말 것 — 남길 게 없으면 안 남기는 게 맞다.
-->

Closes #

<!-- 무엇을 왜 바꿨는지. 한 줄이면 한 줄로. -->

<!-- 갈림길이 있었다면 버린 선택지와 이유. 없으면 이 부분은 통째로 지운다. -->

<!-- 확신 없는 것, 검증하지 않은 것, 일부러 안 한 것. 없으면 지운다. -->
