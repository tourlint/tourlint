.env
.env.*
# 값이 빈 예시 파일은 추적한다 — 새 팀원이 필요한 변수를 알 방법이 이것뿐이다
!.env.example
node_modules/
.pnpm-store/
dist/
.next/
probe_out/
probe2_out/
probe3_out/
probe4_out/

# 에디터 · OS
.idea/
.vscode/
.DS_Store
*.swp

# Claude Code 개인 설정 — 권한 허용 목록 등은 사람마다 다르다.
# 공유할 프로젝트 설정이 생기면 .claude/settings.json 으로 넣는다 (이쪽은 추적한다).
.claude/settings.local.json

# 운영 DB 덤프 · 수동 확인 산출물. 실제 계정 데이터가 들어 있어 저장소에 올리지 않는다
outputs/
.codex-artifact-work/

# python 바이트코드 — scripts/ 를 돌리면 생긴다
__pycache__/
